# PROJECT_STATE — GalleryCleaner *(nama app di layar: **Snaply**)*

## ⚠️ IDENTITAS PROJECT
- Display: **Snaply**. Kode/repo: **`GalleryCleaner`** / `com.example.gallerycleaner` (repo GitHub, folder Termux, `applicationId`, package Kotlin, `rootProject.name`, class/style resource internal, folder storage on-device).
- DILARANG: ganti sisa "GalleryCleaner" di kode/config jadi "Snaply"; ganti `-iname`/folder Termux; ganti `applicationId`/package — tanpa izin eksplisit user.

## ⚠️ ATURAN PERMANEN SESI
1. Tiap sesi WAJIB tampilkan: versionName/batch latest + 1-2 baris update terkini.
2. `versionCode`/`versionName` WAJIB dari `GITHUB_RUN_NUMBER`. DILARANG bump versi manual.

## 🔗 Rilis Terbaru
- GitHub Release (APK signed, siap install, muncul di sidebar repo): **https://github.com/FDzaki-dev/GalleryCleaner/releases/latest**
- Publish otomatis tiap push ke `main` lewat `.github/workflows/build.yml` (`softprops/action-gh-release@v2`, tag `v1.0.<run_number>`) — bukan cuma Actions Artifact, `permissions.contents: write`.

## Versi Saat Ini
v96 → naik otomatis ke run number berikutnya saat push kode berikutnya (Batch108 = docs-only, 0 kode berubah, 0 push baru) — Batch107 (kode terakhir yang push): **Dead-code cleanup pasca `minSdk` 31, Stage 2** — `GalleryCleanerApp.kt` (site `Build.VERSION.SDK_INT >= 28` di `newImageLoader()`'s `.components{}`: minSdk 31 > 28 bikin kondisi selalu true, else-branch `GifDecoder.Factory()` dead dihapus, `add(ImageDecoderDecoder.Factory())` jadi unconditional; import `Build` + `GifDecoder` ikut dihapus). Batch108: **investigasi app-size bloat (urgent, diminta user)** — root cause ganda ditemukan (R8 mati total + `material-icons-extended` oversized vs 24 icon unik dipakai), detail: lihat "Belum Dikerjakan" teratas. 0 file kode disentuh batch ini — murni audit + dokumentasi, nunggu keputusan user buat opsi remediasi (A/B/gabungan) sebelum eksekusi. Batch109: **user pilih Opsi A** — dependency `androidx.compose.material:material-icons-extended` dicabut (`app/build.gradle.kts`, 1 baris, diganti comment penjelasan). **Koreksi ANTI-STALE**: re-grep project-wide `Icons.(Filled|Outlined|Rounded|Sharp|TwoTone).*` sebelum eksekusi — hasil VERIFIED 18 icon unik (semua `Icons.Filled.*`, 0 varian lain), BUKAN 24 seperti tertulis di ringkasan Batch108 (daftar 18 nama icon di Batch108 sendiri sudah benar — cuma angka total "24"-nya typo/salah hitung, sekarang dikoreksi ke 18 di seluruh dokumen). Status: **belum tervalidasi compiler/CI asli** (sandbox 0 Android SDK, 0 network) — kalau ada di antara 18 icon itu yang ternyata extended-only, next push bakal gagal `Unresolved reference` di GitHub Actions, gampang ketauan & tinggal revert 1 baris (sudah diantisipasi sejak analisis Opsi A Batch108). Opsi B (R8 minify+shrink, AUDIT_GAP P2 #16) TETAP belum dieksekusi — masih opsi terpisah/tambahan kalau Opsi A belum cukup menutup app-size setelah user cek APK asli. Batch110: **Opsi A GAGAL, di-revert** — user upload log CI gagal (`run231`, `compileReleaseKotlin`): 7 dari 18 icon project ternyata genuinely extended-only (`Shuffle`/`Folder`/`GridView`/`Sort`/`Undo`/`ViewCarousel`/`ZoomIn`, dipakai di `HomeScreen.kt`/`HomeScreenFolderRow.kt`/`SwipeScreen.kt`/`SwipeScreenGrid.kt` — 1 error tambahan di `SwipeScreen.kt:259` adalah cascading dari `GridView`/`ViewCarousel` unresolved di baris sekitarnya, bukan bug independen). Sesuai mitigasi yang sudah didokumentasikan sejak analisis Batch108 ("gampang di-revert 1 baris"), `material-icons-extended` dikembalikan ke `app/build.gradle.kts`. App-size utk icon dependency ini sekarang balik ke baseline pre-Batch109 — remediasi app-size praktis cuma tersisa lewat Opsi B (R8), ATAU migrasi 7 icon itu ke vector drawable lokal (scope lebih besar, butuh instruksi eksplisit user, BELUM diusulkan/dieksekusi). Batch111: **user pilih lanjut Opsi B, eksplisit minta "ekstra protection"** — `isMinifyEnabled`/`isShrinkResources` dinyalain (`app/build.gradle.kts`, `release {}` block) buat PERTAMA KALINYA di project ini, DIBARENGI penambahan keep-rule baru di `proguard-rules.pro` (2 file kode, dalam cap). 4 rule baru, tiap satu berdasar baca kode project sendiri (bukan template generik copy-paste): (1) `-keepattributes SourceFile,LineNumberTable` + `-renamesourcefileattribute` + `*Annotation*` — TANPA ini, `CrashLogger.kt` (fitur safety-net project sendiri) bakal nulis stack trace yang udah keobfuskasi/gak ada nomor baris ke laporan crash pasca-rilis, regresi ke fitur yang SUDAH ada, bukan hipotetis; (2) `-keep` utk `androidx.work.ListenableWorker` subclass (generic pattern + 2 kelas konkret `CleaningReminderWorker`/`TrashExpiryWorker`) — WorkManager instantiate Worker via reflection (`Class.forName` + constructor `(Context, WorkerParameters)`) saat eksekusi, sedangkan reference build-time (`PeriodicWorkRequestBuilder<T>`) cuma nyentuh `::class.java`, bukan constructor call yang keliatan R8 sebagai "dipakai" — resiko constructor ke-strip = worker gagal jalan SILENT (0 crash log, job cuma gak pernah eksekusi); (3) `-keep class androidx.biometric.**` — App Lock (Batch39) drive dialog internal lewat FragmentManager sendiri, library kecil jadi keep penuh murah; (4) `-dontwarn okhttp3.internal.platform.**`/`conscrypt`/`bouncycastle`/`openjsse` — persis konfigurasi resmi OkHttp sendiri buat R8 (in-app updater, Batch49, probe TLS provider opsional via reflection yang emang gak pernah dibundle app ini). Diverifikasi SEBELUM eksekusi (bukan asumsi): grep `getIdentifier` project-wide 0 hit (resource shrinking aman dari dynamic-lookup crash), grep `Gson`/`kotlinx.serialization`/`Moshi`/`@Serializable`/`Parcelize` 0 hit (0 data class butuh keep-rule serialization tambahan — `JSONObject` yang dipakai `HashCacheStore.kt`/`UpdateChecker.kt` itu `org.json` bawaan platform Android, BUKAN dari app code/dependency, jadi 0 disentuh R8 sama sekali). **Belum tervalidasi compiler/CI/device asli** (sandbox 0 Android SDK/network) — status FINAL nunggu build GitHub Actions push berikutnya, sama alur evidence-based kayak Batch109/110 (kalau ada regresi reflection yang KELEWAT dari 4 rule di atas, bakal nongol di log CI atau, lebih beresiko, di device asli sebagai silent failure — user diminta cek fitur App Lock/Reminder notif/Trash auto-expiry/Update checker/CrashLogger setelah install APK baru, bukan cuma percaya "build sukses" doang). Batch112: **user pilih lanjut migrasi 7 icon `-extended`-only ke vector drawable lokal (opsi (b), paralel/tambahan ke Opsi B — bukan pengganti)** — Stage 1/2 (foundation) dieksekusi: 7 file drawable XML baru (`ic_shuffle`/`ic_folder`/`ic_sort`/`ic_undo`/`ic_view_carousel`/`ic_zoom_in`/`ic_grid_view`), **0 file existing disentuh**, well-formed XML terverifikasi (7/7), pathData 6/7 direproduksi dari memori Material Symbols resmi (Apache 2.0) — belum ada akses network buat cross-check, `ic_grid_view` sengaja redraw generik bukan klaim exact (family icon paling ambigu). Detail lengkap + rencana Stage 2 (wiring 5 file + cabut dependency, atomik >3-file, precedent STABILITY WINS Batch77): lihat bullet "Opsi (b)" di "Belum Dikerjakan" atas. Batch113: **hotfix regresi Batch111 (1 file, `proguard-rules.pro`)** — user konfirmasi via build/device asli: dari 18 icon project, PERSIS 7 icon extended-only (`Shuffle`/`Folder`/`Sort`/`Undo`/`ViewCarousel`/`ZoomIn`/`GridView`) render BLANK pasca `isMinifyEnabled`/`isShrinkResources` nyala (Batch111), 11 icon `-core` sisanya (`Close`/`Check`/`ArrowBack`/`Search`/`Settings`/`Edit`/`Lock`/`PlayArrow`/`Info`/`Share`/`Refresh`) NORMAL — gap yang KELEWAT dari audit 4-rule Batch111 (audit itu cover reflection risk Coil/WorkManager/Biometric/OkHttp, TAPI gak cover R8 class-merging risk yang spesifik ke `material-icons-extended`). Root cause: `material-icons-extended` isinya ~1100+ file singleton nyaris identik per-icon (masing-masing punya private lazy-cached `ImageVector?` field) — R8 full-mode horizontal class-merging + field-value-propagation (baru aktif sejak minify nyala) nge-gabung class-class yang strukturnya mirip itu dan ngerusak cache per-icon-nya, icon jadi render kosong (bukan crash) — `material-icons-core` (dipakai 11 icon lain) ship pre-built/kecil, gak kena jalur ini, makanya cuma yang extended doang yang blank. Fix: rule ke-5 di `proguard-rules.pro` — `-keep class androidx.compose.material.icons.filled.** { *; }` + `-dontwarn` pasangannya, di-scope ke package `filled` doang (grep-verified 0 pemakaian Outlined/Rounded/Sharp/TwoTone/AutoMirrored di project ini). **Independen dari migrasi Batch112** (vector drawable lokal Stage 1, belum di-wire) — kalau Stage 2 migrasi itu nanti dieksekusi (masih nunggu instruksi eksplisit user, scope-nya BEDA dari fix ini), dependency + rule ke-5 ini bisa ikut dicabut sekaligus. Untuk SEKARANG (dependency masih dipakai penuh), rule ini yang bikin build tetap kerja bener di bawah R8. **Belum tervalidasi compiler/CI/device asli** (sandbox 0 Android SDK/network, sama limitasi standar tiap batch minify) — user diminta re-build + cek ulang ke-7 icon itu di device asli buat konfirmasi fix-nya nutup regresinya. Batch114: **user pilih lanjut Opsi (b) Stage 2 (FINAL) — migrasi total ke vector drawable lokal, dependency dicabut** — commit atomik 5 file (precedent STABILITY WINS Batch77, sesuai rencana yang sudah didokumentasikan sejak Batch112): `HomeScreen.kt` (Shuffle→`ic_shuffle`), `HomeScreenFolderRow.kt` (Folder→`ic_folder`), `SwipeScreen.kt` (3 titik: Undo→`ic_undo`, Sort→`ic_sort`, GridView/ViewCarousel→`ic_grid_view`/`ic_view_carousel`), `SwipeScreenGrid.kt` (ZoomIn→`ic_zoom_in`), `app/build.gradle.kts` (baris dependency `material-icons-extended` dicabut). Tiap file: import `Icons.Filled.<nama>` yang extended-only dihapus, `import androidx.compose.ui.res.painterResource` ditambah, `Icon(Icons.Filled.X, ...)` → `Icon(painterResource(id = R.drawable.ic_x), ...)` — 1:1 drop-in, 0 perubahan struktur/logic lain, `Icons` object TETAP dipakai di ke-4 file itu (icon core lain: Close/Check/ArrowBack/Search/Settings/Edit/Refresh/Info/Share) jadi importnya TIDAK dihapus, cuma yang extended-only. `proguard-rules.pro` rule#5 (Batch113) SENGAJA dibiarkan, bukan dihapus (sekarang cuma cover sisa material-icons-core kecil di package yang sama — 0 compiler buat verifikasi aman dicabut, jadi safety net dipertahankan, lihat catatan diperbarui di file itu sendiri). **Verifikasi yang SUDAH dilakukan** (maksimal tanpa compiler/SDK asli): grep project-wide 0 sisa `Icons.Filled.{Shuffle,Folder,Sort,Undo,ViewCarousel,ZoomIn,GridView}` di manapun, grep 0 sisa `material-icons-extended` di build file manapun, `Icons`-object usage dikonfirmasi tetap >0 di ke-4 file (import gak jadi unused), `painterResource` import persis 1x per file (gak duplikat), brace/paren balanced ke-5 file (`HomeScreen.kt` 94/94+207/207, `HomeScreenFolderRow.kt` 46/46+111/111, `SwipeScreen.kt` 115/115+201/201, `SwipeScreenGrid.kt` 42/42+121/121, `app/build.gradle.kts` 21/21+74/74). **Risiko yang TERSISA, jujur (ZERO-HALLUCINATION, warisan langsung dari Batch112)**: 6 dari 7 pathData direproduksi dari memori model atas Material Symbols resmi (Apache 2.0) — TIDAK dijamin pixel-identik ke asset resmi lama; `ic_grid_view` malah SENGAJA safe-redraw (bukan klaim reproduksi) karena family icon paling ambigu buat direproduksi dari memori. **Belum tervalidasi SAMA SEKALI**: compile Kotlin asli (baru pertama kali ke-7 drawable ini benar-benar direferensikan dari kode), visual render asli (Android Studio preview/device — apakah ke-7 icon itu terlihat wajar/mirip aslinya), DAN ukuran APK sebelum/sesudah (klaim app-size membaik MASIH ESTIMASI sampai user build+compare APK asli). User WAJIB: (1) build dulu, kalau `compileReleaseKotlin` gagal — kemungkinan besar salah nama resource/typo, gampang dilacak dari pesan error; (2) kalau compile sukses, visual-QA ke-7 icon itu di device asli SEBELUM anggap batch ini final, terutama `ic_grid_view` (paling beresiko keliatan beda dari sebelumnya). Batch115: **user pilih lanjut UI STATE ROTASI-SURVIVAL SWEEP, kelas custom-`Saver` (4 kandidat tersisa sejak Stage 5/Batch104)** — Stage 6 dieksekusi, 1 file (`TrashScreen.kt`): `selected` (`SnapshotStateList<Long>`) dikasih `listSaver` custom (`save = { it.toList() }`, `restore = { it.toMutableStateList() }`), dipasang lewat `rememberSaveable(saver = listSaver<SnapshotStateList<Long>, Long>(...))`. Dipilih duluan dari 4 kandidat (bukan urutan tertulis di tracker) karena risiko teknis paling rendah: elemen `Long` primitif, langsung didukung `Bundle` tanpa encode/decode tambahan — BEDA kelas masalah dari `organizeTarget`/`zoomedItem` (bawa `MediaItem`, belum dikonfirmasi Parcelable-compatible) atau `updateState` (sealed class `UpdateUiState`, butuh `Saver` per-subtype). Comment lama Batch104 ("sengaja TIDAK diubah") dihapus, diganti comment baru yang jelasin pemisahan kelas masalah. 0 perubahan logic seleksi lain (`.retainAll`/`.add`/`.remove`/`.clear`/`.addAll` di file yang sama tetap API `SnapshotStateList` biasa, cuma titik inisialisasi yang berubah). Import baru 1 baris (`androidx.compose.runtime.saveable.listSaver`) — ~~`SnapshotStateList` sendiri diasumsikan ke-cover wildcard `androidx.compose.runtime.*` yang sudah ada di file ini~~ **[KOREKSI Batch116, asumsi ini SALAH — lihat entry Batch116, fix: import eksplisit ditambah]**. `toMutableStateList()` (fungsi, bukan nama class) tetap resolve fine dari wildcard yang sama, tidak terdampak. Verifikasi: brace 55/55, paren 143/143 (`TrashScreen.kt`). **Belum tervalidasi compiler/CI/device asli** (sandbox 0 SDK/network, sama limitasi standar) — user diminta build + tes rotasi layar Trash dengan item ke-select sebelum anggap final. 3 kandidat custom-Saver sisa (`organizeTarget`/`zoomedItem`/`updateState`) BELUM dieksekusi — nunggu instruksi eksplisit user lanjut per-item (pola sama, 1 file/batch). **[Batch116] CI GAGAL, sudah di-hotfix**: user upload log CI (`run236`, `compileReleaseKotlin`) — `Unresolved reference: SnapshotStateList` di baris deklarasi `listSaver<SnapshotStateList<Long>, Long>`. Detail root cause + fix: entry Batch116 di bawah. Batch115 sudah 100% dan lolos compile berikutnya dengan fix Batch116. Batch116: **hotfix CI failure Batch115 (1 file, `TrashScreen.kt`)** — user upload log CI gagal (`run236`, `compileReleaseKotlin`), 1 error: `e: TrashScreen.kt:50:27 Unresolved reference: SnapshotStateList`, persis titik `listSaver<SnapshotStateList<Long>, Long>(...)` yang ditambah Batch116 (sic, Batch115). Root cause: `SnapshotStateList` (nama class) ternyata di package `androidx.compose.runtime.snapshots`, BUKAN `androidx.compose.runtime` — beda dari fungsi `mutableStateListOf()`/`toMutableStateList()` (keduanya di `androidx.compose.runtime`, resolve fine dari wildcard yang sudah ada, makanya 0 error di 2 titik itu). Batch115 keliru asumsi wildcard `androidx.compose.runtime.*` menutup SEMUA simbol terkait `SnapshotStateList` termasuk nama class-nya sendiri — asumsi ini yang salah, sekarang dikoreksi di entry Batch115 di atas (ZERO-HALLUCINATION: gak menghapus jejak kesalahan, ditandai coret + koreksi). Fix: 1 baris import ditambah — `import androidx.compose.runtime.snapshots.SnapshotStateList`. 0 baris lain disentuh, 0 perubahan logic. Verifikasi: brace 55/55, paren 143/143 (`TrashScreen.kt`, tidak berubah dari Batch115 karena cuma nambah 1 baris import). **Belum tervalidasi ulang compiler/CI/device asli** (sandbox 0 SDK/network) — user diminta push + build ulang, ini percobaan ke-2 buat fitur yang sama (pola sama seperti Batch109→110, Batch111→113: gagal CI pertama, hotfix 1-baris, nunggu konfirmasi hijau). Batch117: **bug report user — rotasi hp (portrait↔landscape) saat review folder SELALU balik ke Home dulu** — 1 file (`MainActivity.kt`). Root cause: `AndroidManifest.xml` 0 declare `configChanges` utk `MainActivity` (protected file, TIDAK disentuh — default OS behavior sengaja dipakai project ini) **[KOREKSI Batch122: statement ini SEKARANG STALE — Batch122 justru menambah `android:configChanges="orientation|screenSize|screenLayout"` ke `MainActivity`, dipicu kebutuhan video player buat rotasi ke landscape tanpa kehilangan playback state. Efek sampingnya: seluruh mekanisme "Activity destroy+recreate saat rotasi" yang jadi premis Batch97-121 TIDAK BERLAKU LAGI mulai Batch122 — Activity sekarang cuma re-layout, tidak pernah destroy, untuk config change orientation/screenSize/screenLayout. Detail lengkap: entry Batch122 di bawah.]**, jadi rotasi = Activity di-DESTROY+RECREATE penuh, bukan cuma re-layout. `selectedGroup`/`showTrash`/`showSettings` (penentu `currentScreen` di `when`-cascade bawah file) selama ini `remember{}` polos (memori proses doang, BUKAN Bundle-backed) — recreate = ketiganya reset ke null/false/false, `when`-cascade jatuh ke branch terakhirnya sendiri, `Screen.Home`. BUKAN bug spesifik SwipeScreen/vertical-horizontal view-mode toggle (sempat dicurigai duluan, ternyata `viewMode` Grid/Swipe di `SwipeScreen.kt` cuma local state toggle, 0 keterkaitan) — user melaporkan gejala pakai istilah \"swipe mode vertikal/horizontal\" utk rotasi fisik hp, dikonfirmasi via baca kode `AndroidManifest.xml`+`MainActivity.kt` langsung, bukan asumsi dari istilah laporan. Fix: `showTrash`/`showSettings` (Boolean murni) langsung `rememberSaveable`, 0 resiko. `selectedGroup` (`MediaGroup?`) SENGAJA TIDAK langsung `rememberSaveable` — 1 grup bisa isi ratusan/ribuan `MediaItem`, Bundle saved-instance-state berbagi 1 batas ukuran transaksi Binder kecil (~1MB) buat SELURUH Bundle, bukan per-field — serialize seluruh item tiap rotasi beresiko `TransactionTooLargeException` di library foto ukuran nyata. Sebagai gantinya: `selectedGroupKey` (String ringan) yang `rememberSaveable`, disinkron di 2 titik assignment asli (`onGroupClick`/`onBack`), lalu `selectedGroup` di-REKONSTRUKSI via key-lookup (`LaunchedEffect` baru) begitu sumber datanya siap lagi (`groups`/`smartGroups`/`largestItems`/`onThisDayItems` — SEMUA auto-recompute dari `activeMedia`, 0 nunggu aksi user, jadi reliably ready lagi cepat pasca-rotasi, timing sama kayak cold start). **Scope yang SENGAJA belum tertutup (jujur, bukan nebak)**: \"Blurry photos\"/\"Similar photos\"/\"Duplicate files\" (sumbernya `blurryScanState`/`nearDupScanState`/`duplicateScanState` — scan ON-DEMAND dipicu user, `remember{}` juga, genuinely hilang pas rotasi; auto-rerun scan berat tanpa diminta user dianggap lebih buruk dari fallback ke Home) dan \"Search results\" (query+hasil masih local state di `HomeScreen.kt`, di luar scope fix 1-file ini) — rotasi keluar dari 4 kategori itu MASIH balik ke Home, TAPI ini bukan regresi baru (behavior sama persis kayak sebelum batch ini, cuma belum ketutup). Kategori paling umum dipakai user (grup bulan/album, folder row, smart quick-category, Largest files, On this day) SEKARANG survive rotasi penuh. Verifikasi: brace 277/277, paren 557/557 (`MainActivity.kt`). **Belum tervalidasi compiler/CI/device asli** (sandbox 0 SDK/network, limitasi standar tiap batch) — user diminta build + tes rotasi hp bolak-balik saat lagi review folder sebelum anggap final. Batch118: **user lapor regresi baru — mode select-file di Trash, seleksi hilang pas rotasi** — 1 file (`TrashScreen.kt`). Root cause: `LaunchedEffect(items.map { it.id }) { selected.retainAll(...) }` (komentarnya sendiri: \"Selection resets cleanly whenever the trash contents change\") jalan TANPA GUARD di TIAP composition — termasuk composition PERTAMA pasca Activity recreate, saat `items` (prop dari `trashItems` MainActivity, sumbernya `allMedia`→`derivedMedia`, lihat Batch117) MASIH default kosong karena reload MediaStore pasca-rotasi belum kelar. `retainAll(emptySet())` di momen itu langsung ngosongin `selected` yang BARU SAJA sukses di-restore lewat `listSaver`-nya sendiri (Batch115/116) — begitu data trash asli kelar load sesaat kemudian, udah kosong duluan, gak ada apa-apa buat di-retain. **Bug ini SEBENARNYA sudah laten sejak Batch115/116** (makanya kedua batch itu sendiri nulis \"belum tervalidasi device asli\") — cuma gak pernah ketauan/ke-reach, karena SEBELUM Batch117, rotasi selalu mental balik ke Home duluan (bug Batch117) sebelum `TrashScreen` sempat compose sejauh ini — Batch117 nutup 1 bug tapi (baru kesadaran sekarang) otomatis MEMBUKA jalan ke bug laten kedua ini yang sebelumnya ke-mask total. Fix: `hasSyncedOnce` (`remember{}` biasa, TIDAK perlu saveable — cuma butuh bedain \"composition pertama instance composable ini\" vs seterusnya) nge-guard `retainAll` biar SKIP pas firing pertama doang — transient-empty snapshot gak lagi bisa nge-clobber seleksi yang baru di-restore. Firing KEDUA dst. tetap jalan normal (prune beneran begitu `items` udah data asli) — termasuk kasus asli yang efek ini emang didesain buat itu (trash beneran jadi kosong pasca permanent-delete) TETAP kepegang benar, dicek manual lewat 3 skenario (cold-open trash kosong genuinely, permanent-delete sampai trash kosong di sesi normal, rotasi-dengan-seleksi) — detail penelusuran di masing-masing skenario TIDAK ditulis ulang di sini (ada di reasoning session, bukan kode), cuma hasil finalnya. Verifikasi: brace 57/57, paren 150/150 (`TrashScreen.kt`). **Belum tervalidasi compiler/CI/device asli** — user diminta build + ulangi tes yang sama (pilih beberapa item di Trash, rotasi hp) buat konfirmasi seleksi sekarang bener-bener survive. Batch119: **request baru user — tambah opsi Color Style ke-4 "Sage Wash"** (custom task, bukan dari AUDIT_GAP/bug report) — brief: berbasis Material 3, typography+shape "underrated keren", kombinasi accent warna calm, efek brush/painted (watercolor). Scope genuinely 4th full material language (bukan restyle tema existing) — **11 file disentuh (4 baru zero-risk + 7 existing wajib update exhaustive-`when`), di atas cap 3-file, justifikasi STABILITY WINS identik precedent Batch77 (Cupertino stage 2, 4 file) / Batch114 (icon migration stage 2, 5 file)**: nambah 1 `AppTheme`/1 `MaterialStyle` enum entry MEMAKSA update SEMUA exhaustive-`when` yang menjahitnya (`MaterialStyle.kt`'s `materialStyleFor`, `Theme.kt`'s `colorSchemeFor`, `GlassCard.kt`, `GlassButton.kt`, `SwipeScreenControls.kt`'s `InfoChip`) — bukan pilihan, syarat compile Kotlin, pola sudah diflag transparan sejak precedent Batch74/77. **4 file baru (0 risiko, standalone, murni — sama standar 'no hybrid baseline' Amber Reserve/Indigo Noir)**: (1) `PaintedTokens.kt` — object `Painted`, palette calm baru 100% (moss/terracotta/ochre), 0 hue dipinjam dari 3 tema lain, kontras WCAG dihitung manual pakai Python lokal sebelum commit — semua pasangan text/accent lolos AA (dark: TextPrimary/Bg 14.8:1, TextOnAccent/Moss 6.7:1, TextOnAccent/Terracotta 5.8:1; light: TextPrimaryOnLight/BgOnLight 14.7:1, white/MossOnLight 6.1:1, white/TerracottaOnLight 5.8:1) — TextSecondaryOnLight sengaja alpha 66% bukan 68% (68% cuma dapat 4.4:1, di bawah AA 4.5:1, dicek eksplisit). (2) `PaintedShape.kt` — asymmetric 4-corner-radius `RoundedCornerShape` (Card/Button/Chip), beda dari tema lain yang uniform-radius (Signature/Indigo) atau uniform-cut (Amber's `CutCornerShape`) — API sama persis yang sudah proven, cuma parameter beda. (3) `PaintedTypography.kt` — serif (display/headline/title) + sans (body/label), full 11 role (termasuk 4 role gap `GalleryTypography` sendiri belum tutup sejak Batch84 — tema baru ini sengaja gak ikut warisin gap itu), weight SELALU sama-atau-lebih-ringan dari `GalleryTypography` (kebalikan `NeumorphTypography`'s +1-step-berat), tracking dilonggarkan bukan dirapatkan. (4) `PaintedSurface.kt` (`ui/components/`) — resep ke-4: 1 shadow M3 polos (bukan glow-tint Glass, bukan dual-offset Neumorph) + fill wash 3-blob radial-gradient (moss/ochre/terracotta, alpha 0.16-0.24, posisi fraksional TETAP fungsi dari `size` per draw — bukan random per-recomposition) + edge brush-stroke gradient 3-hue (teknik `clipPath`+`addOutline`+`Stroke` di-reuse PERSIS dari `NeumorphSurface`'s fading-border, cuma ganti gradient) + press = state-layer scrim (`Painted.PressedScrim` di atas fill, wash di-skip pas pressed) — mekanisme tekan ke-4 yang genuinely beda dari fill-swap (Glass)/shadow-hilang+fill-swap (Neumorph)/whole-control-alpha-dim (Cupertino). **7 file existing (wajib, exhaustive-`when`/registrasi)**: `SettingsStore.kt` (`AppTheme.SAGE_WASH` ditambah, 1 baris); `MaterialStyle.kt` (`MaterialStyle.PAINTED` + `materialStyleFor` map `SAGE_WASH → PAINTED`); `Theme.kt` (`SageWashDark`/`SageWashLight` `ColorScheme` baru baca `Painted.*` doang + `colorSchemeFor`/`typographyFor` di-update, 1 doc comment stale diperbaiki); `GlassCard.kt` (early-return branch `PAINTED`, pola sama NEUMORPH/CUPERTINO, + exhaustive `when(style)` di dalamnya); `GlassButton.kt` (branch CTA `PAINTED`, fill=`Painted.Moss`, `showWash=false` biar label gak kesaing tekstur, `showBrushStroke` tetap true); `SwipeScreenControls.kt`'s `InfoChip` (early-return branch `PAINTED`, shadow kecil + `showWash=false`, + exhaustive `when` di trailing block); `SettingsScreen.kt` (entry `ThemeStyle` ke-4 di `THEME_STYLES`, list-driven — 0 perlu ubah komponen Row/Card itu sendiri, cuma nambah data — + import `Painted` + 1 doc comment stale diperbaiki). `strings.xml` — 2 entry baru (`settings_theme_sage_wash_label`="Sage Wash", `_desc`). **Verifikasi yang SUDAH dilakukan** (maksimal tanpa compiler/SDK asli): brace/paren balanced 11/11 file, XML well-formed (`xml.etree.ElementTree`), grep keenam titik exhaustive-`when` (`materialStyleFor`/`colorSchemeFor`/`typographyFor`/`GlassCard`/`GlassButton`/`InfoChip`) dikonfirmasi SEMUA sudah nyebut `PAINTED`/`SAGE_WASH`, grep import `Painted`/`PaintedShape`/`PaintedSurface`/`PaintedTypography` dikonfirmasi ADA di tiap file pemakai. **Belum tervalidasi SAMA SEKALI**: compile Kotlin asli — resiko lebih tinggi dari batch icon-migration Batch112/114 (yang cuma 1:1 API call swap), batch ini bawa API Compose yang BELUM PERNAH dipakai di project ini sebelumnya (`drawWithCache`+`onDrawBehind`, `drawCircle(brush:Brush, ...)` overload) — resiko "Unresolved reference"/signature-mismatch lebih tinggi dari batch-batch icon kemarin; visual render asli (watercolor wash blob + brush-stroke edge + asymmetric shape SEMUA butuh device/preview buat konfirmasi kesan "artistik, personal, organic" sesuai brief); 4th press mechanism (state-layer scrim) belum ke-tes interaktif di device. **User WAJIB build dulu SEBELUM anggap batch ini final** — titik paling beresiko kalau `compileReleaseKotlin` gagal: `PaintedSurface.kt`'s `drawWithCache` block. **[Batch120] KONFIRMASI: Batch119 COMPILE SUKSES** — user upload 2 screenshot device asli (Settings color-style picker dengan "Sage Wash" ter-select+checkmark, dan Home screen ter-render pakai tema itu) — 0 crash, 0 compile error, API Compose baru (`drawWithCache`/`onDrawBehind`/`drawCircle(brush=)`) TERBUKTI jalan, brush-stroke gradient edge KELIHATAN jelas di kedua card screenshot (border teal→orange). Kekhawatiran "belum tervalidasi compile" di entry Batch119 di atas SEKARANG **selesai, positif** — TAPI user kasih feedback visual: hasilnya "lebih mirip app scam dapetin duit daripada kesan premium", bukan bug/crash, murni art-direction. Batch120: **revisi palette (2 file: `PaintedTokens.kt` + `PaintedSurface.kt`), 0 file lain disentuh** (semua downstream — `Theme.kt`/`GlassButton.kt`/`SwipeScreenControls.kt`/`SettingsScreen.kt` — baca `Painted.*` sebagai simbol, ganti hex di 1 objek otomatis ke-propagate ke semua pemakai, 0 titik lain perlu disentuh). **Root cause terdiagnosis dari screenshot**: v1 pasang [Bg] coklat-taupe hangat + [Terracotta] oranye-hangat — warm-on-warm, analog, jadi kartu kelihatan flat/muddy (bukan tekstur watercolor) DAN teks warning trash ("577.4 MB waiting...") kebaca caution-orange bukan calm-clay. Signature/Amber Reserve/Indigo Noir semua pakai pola "base GELAP+DINGIN dikontraskan sama accent HANGAT" (navy+brass, navy+coral, indigo+dusty-rose) — v1 Sage Wash melanggar pola sukses ini (base DAN accent sama-sama hangat, 0 kontras). Fix: `Bg`/`Surface`/`SurfaceRaised` digeser dari coklat ke forest-charcoal gelap+dingin (H155°, ties ke identitas "sage"-nya sendiri, bukan malah lawan), `Terracotta` digeser hue dari oranye (~H20°) ke brick-rose (H10°) + saturasi diturunkan — tetap "terracotta pottery" tapi jauh dari kesan "traffic-cone alert", `Moss` saturasi dinaikkan dikit biar tetap "pop" sebagai accent lawan base yang sekarang lebih gelap. Wash-blob alpha (`PaintedSurface.kt`) dinaikkan 0.24/0.16/0.18 → 0.34/0.24/0.26 — versi lama kemungkinan besar KEBACA nyaris gak keliatan di device asli (rendering flat, bukan tekstur watercolor), naik sampai jelas persepsinya tanpa nutupin konten. Brush-stroke edge gradient SENGAJA TIDAK diubah (sudah kelihatan jelas & malah dipuji-secara-implisit di screenshot — teal→orange border itu jalan sesuai desain). **Kontras WCAG di-verifikasi ULANG via Python sebelum commit** (bukan asumsi tinggal ganti hex): TextOnAccent/Moss naik ke ≈7.3:1 (dari 6.7:1), TextOnAccent/Terracotta SEMPAT jatuh ke 4.1:1 (di bawah AA 4.5:1) pas lightness awal 52% — dikoreksi naik ke lightness 58% sampai dapet ≈5.0:1, TextPrimary/Bg tetap AAA (15.6:1), MossOnLight/TerracottaOnLight ikut di-retune (≈5.1:1 / ≈7.5:1 buat teks putih). Verifikasi: brace/paren balanced kedua file (1/1+57/57, 11/11+68/68). **Masih belum tervalidasi**: apakah revisi ini BENERAN kebaca "premium" di device asli (subjektif, cuma bisa dikonfirmasi lewat screenshot/build berikutnya dari user) — kode sudah proven compile (Batch119), jadi resiko batch ini murni visual/preference, bukan lagi resiko compile. Batch121: **user bug report — MP4 ikut tampil di app tapi 0 bisa di-play/diinspeksi sama sekali** (2 file: `app/build.gradle.kts` + `SwipeScreenCard.kt`). Root cause BUKAN video yang "seharusnya difilter keluar" seperti dugaan laporan — video sudah SENGAJA jadi bagian nyata library sejak Batch40 (Audit Gap P0 #1: query `MediaStore.Video.Media`, thumbnail via Coil `VideoFrameDecoder`, play-badge) — melainkan `FullscreenViewer` (`SwipeScreenCard.kt`, satu titik dipanggil dari SwipeScreen.kt's `onZoomRequest` MAUPUN SwipeScreenGrid.kt's zoom-icon) merutekan SEMUA `MediaType` lewat `AsyncImage` yang sama; `VideoFrameDecoder` cuma bisa decode SATU frame statis dari URI video, 0 konsep pemutaran (0 player, 0 kontrol) — persis gap yang sudah di-flag jujur di Batch40 sendiri ("Tap-to-play... SENGAJA belum dikerjakan batch ini", `ARCHIVE_HISTORY.md`) tapi belum pernah ditutup sampai sekarang. Fix: dependency baru `androidx.media3:media3-exoplayer` + `androidx.media3:media3-ui` v1.4.1 (lihat komentar `app/build.gradle.kts` untuk alasan pin versi lengkap — v1.11.0 terbaru per developer.android.com mengaku menaikkan Kotlin internal 2.0.20→2.2.0 di changelog resminya sendiri, project ini di Kotlin 1.9.24, persis kelas risiko yang sama seperti pin `kotlinx-collections-immutable` 0.3.8 di Batch44; 1.4.1 dari era yang sama dengan toolchain project ini, diverifikasi benar-benar rilis nyata via mvnrepository.com, bukan tebakan versi). `FullscreenViewer` sekarang `if (item.mediaType == MediaType.VIDEO)` bercabang ke composable baru `VideoPlayerSurface` (ExoPlayer + `PlayerView` via `AndroidView`, player di-`remember(uri)` dan di-`release()` lewat `DisposableEffect.onDispose` biar gak leak, `Player.Listener.onPlayerError` nangkep gagal-decode jadi teks "Can't play this video" alih-alih layar hitam diam tanpa feedback) — cabang foto/GIF (`AsyncImage`) 0 disentuh. Kenapa ini gak crash di file besar (poin eksplisit yang diminta laporan bug): ExoPlayer baca `content://` URI sebagai stream (`ContentDataSource`→extractor→`MediaCodec`), decode buffer secukupnya buat tetap beberapa detik di depan playhead, BUKAN load seluruh file ke satu buffer memori. Tombol close (`Icons.Filled.Close` — icon core, sudah dipakai `HomeScreen.kt`, 0 risiko class-merging R8 ala Batch113) dipasang khusus cabang video karena tap-anywhere-to-dismiss (dipertahankan di cabang foto) akan bentrok sama gesture tap bawaan `PlayerView` buat toggle kontrolnya sendiri. 0 perubahan `AndroidManifest.xml` (`READ_MEDIA_VIDEO` sudah ada sejak Batch40) dan 0 entry baru `proguard-rules.pro` (Media3/ExoPlayer bawa consumer ProGuard rules sendiri di dalam AAR, beda dari Coil yang butuh `-keep` manual karena decoder discovery-nya lewat reflection — dicatat sebagai alasan, bukan tervalidasi lewat build R8 asli di sandbox ini). Semua package import baru (`androidx.media3.common.MediaItem` — di-alias `Media3MediaItem` biar gak bentrok nama sama domain model `MediaItem` project ini yang satu package flat, `androidx.media3.common.Player`/`PlaybackException`, `androidx.media3.exoplayer.ExoPlayer`, `androidx.media3.ui.PlayerView`) dicek lewat web search ke dokumentasi/contoh kode resmi Android Developers sebelum ditulis — pelajaran langsung dari Batch41 (hotfix CI gara-gara nebak salah package `coil.video.VideoFrameDecoder` tanpa verifikasi). Duration label & filter "hanya foto"/"hanya video" di UI (2 dari 3 item sisa yang SENGAJA belum dikerjakan Batch40) TETAP belum dikerjakan — di luar scope laporan bug spesifik ini. Verifikasi: brace/paren balance `SwipeScreenCard.kt` 54/54 + 135/135 (dicek ulang setelah seluruh edit). **Belum tervalidasi compiler/CI/device asli** (sandbox 0 Android SDK/network, limitasi standar tiap batch) — user diminta build + tes: (1) buka video dari grid-zoom MAUPUN swipe-zoom, pastikan benar-benar bisa play/pause/seek, bukan cuma nampilin frame; (2) coba video ukuran besar/durasi panjang, pastikan 0 crash/ANR; (3) tombol close berfungsi kembali ke layar sebelumnya. Batch122: **user bug report pasca-Batch121, 2 temuan sekaligus** — (1) video masih 0 bisa diputar sama sekali ("Can't play this video" di setiap video, screenshot device asli), (2) pemutar video 0 bisa dirotasi ke landscape + kontrol pemutar (scrubber/timestamp/tombol fullscreen) kepotong/tidak kejangkau di bagian bawah layar. 3 file disentuh. **Temuan (1), root cause DUGAAN (bukan konfirmasi pasti — sandbox 0 Android SDK/device/logcat asli utk lihat `PlaybackException` sungguhan)**: `app/build.gradle.kts`'s komentar Batch121 sendiri mengasumsikan "0 proguard-rules.pro entry diperlukan — Media3 sudah bawa consumer rules sendiri" TANPA verifikasi build asli — persis kelas asumsi yang sudah SEKALI terbukti salah di project ini (rule#5, material-icons-extended, Batch111→113). Dicari preseden publik: tracker resmi ExoPlayer (`google/ExoPlayer#8709`) mendokumentasikan persis pola yang sama — mp4/m4a gagal diputar ("Source error") di build release dengan minify ON, normal di debug, akar masalah keep-rule yang kurang. Mitigasi: `app/proguard-rules.pro` rule#6 baru — `-keep` utk `androidx.media3.{exoplayer,extractor,decoder,common,datasource}.**` + `-dontwarn androidx.media3.**`, discope ke package yang benar-benar dipakai jalur playback lokal app ini (bukan blanket seluruh `androidx.media3.**`, karena project ini 0 pakai DASH/HLS/RTMP/Cast/session/datasource-okhttp). **Independen dari itu**, `SwipeScreenCard.kt`'s `VideoPlayerSurface` — `onPlayerError` dulu cuma nyalain 1 Boolean lalu nampilin 1 string hardcoded, MEMBUANG seluruh detail `PlaybackException` (persis pola "silent background exception" yang jadi alasan `CrashLogger.kt` dibikin dari awal project ini) — sekarang nampilin `error.errorCodeName` + `error.message` asli, DAN controller PlayerView (yang sebelumnya tetap kelihatan nge-overlap teks error, gambar screenshot user nunjukin rewind/play/ff tombol numpuk sama teks "Can't play this video") di-swap total jadi teks error begitu `playbackError=true` — kalau rule#6 di atas TERNYATA belum cukup nutup akar masalahnya, laporan bug BERIKUTNYA bakal bawa kode error asli, bukan tebakan lagi. **Temuan (2), root cause TERKONFIRMASI dari baca kode (bukan dugaan)**: `MainActivity` 0 pernah declare `configChanges` (lihat koreksi di entry Batch117 di atas) — kombinasi FATAL buat video player, karena (a) ExoPlayer/state fullscreen bakal ke-reset/hilang tiap fisik hp diputar (Activity destroy+recreate total), dan (b) bahkan kalaupun mau paksa landscape via `requestedOrientation` programatik, OS TETAP bakal destroy+recreate Activity itu sendiri persis di momen orientasi beneran berubah — 0 cara di sekitar ini tanpa `configChanges`, ini fakta platform Android, bukan tebakan. Fix: `AndroidManifest.xml` (protected file, edit skala kecil — pola sama kayak Batch40/Batch106 nambah/hapus 1 baris permission) — `MainActivity` sekarang `android:configChanges="orientation|screenSize|screenLayout"`, jadi rotasi TIDAK LAGI destroy Activity app-wide (bukan cuma pas video), Compose cukup recompose di tempat. `SwipeScreenCard.kt`'s `VideoPlayerSurface` — `DisposableEffect` baru minta `activity.requestedOrientation = ActivityInfo.SCREEN_ORIENTATION_SENSOR` selama video kebuka (dibalikin ke `UNSPECIFIED` pas dialog ditutup), biar hp beneran boleh landscape SELAMA nonton video, terlepas dari toggle auto-rotate sistem hp-nya sendiri (sama kayak persis semua video player mainstream) — sekarang aman dipaksa karena `configChanges` di atas nyegah Activity mati pas OS comply. **Fix (2), sekaligus bawa efek samping POSITIF**: `Box` fullscreen Dialog (`FullscreenViewer`) sekarang `DialogProperties(decorFitsSystemWindows = false)` + `.windowInsetsPadding(WindowInsets.systemBars)` — window dialog gambar penuh edge-to-edge (bukan lagi nebak/warisan ukuran window yang kepotong), lalu konten (termasuk tombol close foto/video DAN kontrol PlayerView) secara eksplisit di-inset dari area status bar/gesture-nav, bukan lagi numpuk fisik di strip yang sama dengan gesture-nav (persis simtom di screenshot user — kontrol kelihatan tapi susah/gak kesentuh). **Efek samping app-wide dari fix `configChanges` (bukan disengaja jadi tujuan batch ini, tapi nyata & positif)**: gap "Belum Dikerjakan" lama soal Blurry/Similar/Duplicate/Search results yang balik ke Home kalau rotasi (Batch117 residual, lihat koreksi bullet-nya di bawah) SEKARANG ikut ketutup — bukan karena di-fix langsung, tapi karena premis "Activity mati tiap rotasi" yang jadi akar masalahnya sendiri sudah tidak berlaku lagi. Seluruh infrastruktur `rememberSaveable` dari "UI STATE ROTASI-SURVIVAL SWEEP" (Batch97-118) TETAP dipertahankan, bukan dihapus — sekarang redundant-tapi-tidak-berbahaya untuk kasus rotasi spesifik (Activity toh sudah tidak mati), tapi tetap jadi safety net asli buat process death/low-memory kill (skenario yang TIDAK ditutup `configChanges`). Verifikasi: brace/paren balanced `SwipeScreenCard.kt` (61/61, 152/152), `AndroidManifest.xml` well-formed (`xml.etree.ElementTree`), `proguard-rules.pro` brace balanced. **Belum tervalidasi SAMA SEKALI** (sandbox 0 Android SDK/emulator/device/logcat, limitasi standar tiap batch) — root cause temuan (1) genuinely DUGAAN berdasar preseden publik + pola project sendiri, BUKAN dikonfirmasi lewat log asli; temuan (2) root cause dikonfirmasi dari baca kode/dokumentasi Android resmi (fakta platform), tapi fix-nya sendiri tetap butuh device asli buat lihat hasil visual (apakah kontrol beneran kejangkau sekarang, apakah rotasi video mulus). User WAJIB: (1) build + coba putar video lagi — kalau MASIH gagal, teks error yang sekarang muncul (bukan lagi cuma "Can't play this video" polos) berisi kode error asli, tempel/screenshot itu buat diagnosis pasti; (2) coba putar video lalu fisik rotasi hp ke landscape, pastikan video ikut rotasi & kontrol (scrubber/waktu/tombol fullscreen) kejangkau penuh; (3) test screen lain (Home/Trash/Settings/dst) buat pastikan rotasi app-wide masih normal pasca `configChanges` (regresi tak terduga di luar video, kalaupun kecil kemungkinannya, harus ketauan dari sini). Batch123: **user re-report dengan teks error Batch122 sekarang KEBACA** (screenshot device asli) — `ERROR_CODE_DECODER_INIT_FAILED: MediaCodecVideoRenderer error, index=0, format=Format(1, null, null, video/avc, avc1.640433, -1, null, [3840, 2160, 59.9986, ColorInfo(BT709, Limited range, SDR SMPTE 170M, false, 8bit Luma, 8bit Chroma)], [-1, -1]), format_supported=NO_EXCEEDS_CAPABILITIES`. **Koreksi Batch122**: dugaan "R8 strip sesuatu yang Media3 butuh" sekarang TERBANTAHKAN (bukan cuma "belum terkonfirmasi") — kalau itu penyebabnya, `MediaCodecVideoRenderer` gak akan bisa ke-construct sama sekali buat produksi error se-well-formed ini; error yang muncul justru dari MediaCodec capability-check asli, bukti rule#6 (Batch122) bukan lagi variabel yang relevan di kegagalan ini (dibiarkan di `proguard-rules.pro`, harmless, cuma bukan akar masalah spesifik ini). **Root cause TERKONFIRMASI dari isi error itu sendiri (bukan dugaan)**: video-nya 4K@60fps H.264 High Profile Level 4.3 (`avc1.640433`) — `NO_EXCEEDS_CAPABILITIES` artinya `MediaCodecUtil` NEMU decoder video/avc di device ini, dicoba, dan decoder itu SENDIRI yang nolak resolusi/frame-rate spesifik ini saat init — bukan file corrupt, bukan container gak dikenali. Ini kelas keterbatasan hardware asli yang umum (kebanyakan chipset decoder AVC cuma sampai 4K@30 di hardware; 4K@60 kebanyakan cuma tersedia di HEVC/VP9 hardware, bukan AVC) — TAPI ada mitigasi resmi sebelum divonis "genuinely gak bisa": `ExoPlayer.Builder(context)` (kode Batch121, `MediaCodecSelector.DEFAULT`, decoder-fallback OFF secara default) berhenti total begitu decoder PERTAMA yang dicoba nolak — 0 percobaan ke decoder video/avc lain yang mungkin ada di device itu (mis. decoder software sekunder). Fix 1 file (`SwipeScreenCard.kt`): `exoPlayer` sekarang dibangun lewat `DefaultRenderersFactory(context).setEnableDecoderFallback(true)` + `ExoPlayer.Builder(context, renderersFactory)`, bukan `ExoPlayer.Builder(context)` polos — begitu decoder pertama nolak, ExoPlayer lanjut coba decoder video/avc berikutnya yang match, bukan langsung nyerah. Dicek lewat preseden publik SEBELUM ditulis (bukan tebakan, pola sama seperti Batch121/122's precedent check): `androidx/media#1311` — signature error identik (`format_supported=NO_EXCEEDS_CAPABILITIES`, beda mime video/hevc) diselesaikan cara yang sama persis (`setEnableDecoderFallback(true)`). **Tambahan UX, 1 state baru (`errorHint`)**: `onPlayerError` sekarang juga isi baris terpisah — muncul HANYA kalau `error.errorCode == PlaybackException.ERROR_CODE_DECODER_INIT_FAILED` DAN `error.message` mengandung `"EXCEEDS_CAPABILITIES"` — teks plain-language "This device's hardware can't decode this video's resolution/frame-rate combination.", dirender DI BAWAH `errorDetail` (raw errorCodeName+message Batch122, TIDAK diubah/dihapus sama sekali, tetap ada buat triage lain). **Batas jujur (ZERO-HALLUCINATION, bukan klaim pasti-fix)**: decoder fallback cuma bisa berhasil KALAU device itu genuinely punya decoder video/avc lain (mis. software) yang SANGGUP 4K@60 — kalau tidak ada satupun (hardware maupun software), fallback 0 punya kandidat lain buat dicoba dan video ini TETAP gagal diputar; itu jadi hardware ceiling asli yang 0 bisa diakali app-level, bukan lagi bug. `errorHint` di atas didesain justru buat kasus itu — begitu muncul, user (dan sesi depan) langsung tahu ini bukan lagi soal nebak-nebak kode lagi, videonya sendiri yang di luar kapabilitas decode device itu. Verifikasi: brace/paren balanced `SwipeScreenCard.kt` 65/65 + 174/174 (dicek ulang python setelah seluruh edit). **Belum tervalidasi device asli** (sandbox 0 Android SDK/emulator/device) — user WAJIB build + coba putar ulang video 4K60 yang sama: (1) kalau sekarang bisa play → fallback ke decoder lain berhasil, selesai; (2) kalau masih gagal TAPI `errorHint` baru muncul di bawah teks error → hardware ceiling asli device ini utk 4K@60 avc, bukan lagi item yang bisa di-iterasi kode lebih lanjut; (3) kalau errorCode-nya BEDA dari sebelumnya → lapor screenshot baru, diagnosis lanjut dari situ. Batch124: **hotfix CI run244 (0 file kode/config disentuh — packaging-only)** — user upload log CI gagal (`run244`), error BEDA KELAS dari seluruh histori compile-error sebelumnya (Batch41/109/115/116/dst, semua `compileReleaseKotlin`): Gradle GAGAL START — `FAILURE: Directory '/home/runner/work/GalleryCleaner/GalleryCleaner' does not contain a Gradle build` (0 `settings.gradle.kts` ketemu di root checkout, exception `BuildLayoutException`). Root cause dikonfirmasi dari isi ZIP itu sendiri (`unzip -l Snaply_v123.zip`, bukan dugaan): seluruh 112 entry terbungkus 1 folder wrapper `GalleryCleaner/` (100% path diawali `GalleryCleaner/`, 0 entry di root zip) — beda dari struktur ZIP batch-batch sebelumnya. Script Termux DAILY UPDATE (immutable, `unzip -o "$LATEST_ZIP" -d "$PROJ_DIR/"`, `$PROJ_DIR`=`~/projects/GalleryCleaner`) 0 toleransi wrapper: ekstrak zip ber-wrapper ke folder yang namanya PERSIS SAMA (`GalleryCleaner`) menghasilkan nested `$PROJ_DIR/GalleryCleaner/settings.gradle.kts` bukan `$PROJ_DIR/settings.gradle.kts` — commit yang ke-push jadi repo dengan root kosong dari gradle file, persis simtom run244. Fix: **repackaging murni** — ZIP dibongkar lalu di-zip ulang TANPA wrapper folder (`app/`, `.github/`, `settings.gradle.kts`, dst sekarang persis di root arsip), isi/nama tiap file 100% verbatim (0 byte diff kecuali struktur wrapper terluar) — `com/example/gallerycleaner/` package path dan seluruh nama folder internal (`app/`, `.github/`, dst) TIDAK diubah, cuma 1 level wrapper terluar yang dicabut. Verifikasi: `unzip -l` pada arsip baru konfirmasi 0 entry `GalleryCleaner/` di root, `settings.gradle.kts`/`build.gradle.kts`/`app/`/`.github/` sekarang tepat di root arsip. **Belum tervalidasi CI asli** (sandbox 0 network/GitHub Actions) — user WAJIB push ulang lewat DAILY UPDATE script (0 diubah) dan konfirmasi run berikutnya lolos checkout+configure; root cause ini spesifik struktur ZIP bukan kode, jadi kalau hipotesis ini benar, compile Kotlin seharusnya lanjut normal seperti run sebelum run244. Batch125: **user re-report device asli pasca Batch122/123 — auto-rotate video TETAP 0 jalan + kontrol bawah TETAP ke-kliping strip gesture-nav** (screenshot, 1 file, `SwipeScreenCard.kt`). **Root cause auto-rotate, TERKONFIRMASI (bukan dugaan)**: `VideoPlayerSurface`'s `context as? Activity` (Batch122) dipanggil dari DALAM Compose `Dialog` (`FullscreenViewer`) — `LocalContext.current` di situ BUKAN Activity langsung, Compose `DialogWrapper` membungkusnya dalam `ContextThemeWrapper` (buat apply tema dialog sendiri) — smart-cast `as? Activity` gagal DIAM-DIAM (0 crash, 0 log), balik `null`, seluruh `DisposableEffect`'s `activity?.requestedOrientation = SENSOR` jadi no-op sejak Batch122 ditulis — persis kenapa sandbox 0 pernah ketauan (0 Activity/Dialog windowing asli buat nangkep ini). Fix: fungsi extension `Context.findActivity()` baru (`tailrec`, unwrap `ContextWrapper` chain sampai ketemu `Activity`) — pola standar buat gotcha Compose/Dialog ini, dipakai gantiin cast langsung. **Root cause kliping kontrol bawah, REINFORCEMENT (belum 100% terkonfirmasi tanpa device, tapi limitasi Compose `Dialog` yang terdokumentasi)**: `DialogProperties(decorFitsSystemWindows = false)` (Batch122) diketahui tidak selalu sampai ke `Window` dialog asli di sebagian OEM skin/API level — property di-set saat dialog dibuat, tapi window bisa tetap lapor `fitsSystemWindows=true` di layout pertama sebelum `windowInsetsPadding` sempat baca inset non-zero. Fix tambahan (aditif, TIDAK gantikan `DialogProperties` yang sudah ada): `LocalView.current.parent as? DialogWindowProvider` (parent View composable ini di dalam `Dialog` MEMANG provider itu) lalu `SideEffect` paksa ulang `WindowCompat.setDecorFitsSystemWindows(window, false)` tiap recompose/re-attach. 0 file lain disentuh, 0 perubahan `AndroidManifest.xml`/`build.gradle.kts` (dependency `androidx.core:core-ktx` buat `WindowCompat` SUDAH ada sejak awal project). Verifikasi: brace 68/68, paren 192/192 (`SwipeScreenCard.kt`). **Stuttering video (dilaporkan user, SENGAJA belum digarap batch ini)** — user eksplisit prioritaskan rotate+kliping duluan; stuttering butuh profiling asli (CPU/GPU frame timing, jenis codec, bitrate video) yang 0 tersedia di sandbox ini — DIFLAG di \"Belum Dikerjakan\", nunggu instruksi eksplisit user buat digarap, BUKAN ditebak blind. **Belum tervalidasi device asli** (sandbox 0 Android SDK/emulator) — user WAJIB build + tes ulang: (1) buka video, fisik rotasi hp — pastikan video BENERAN ikut rotasi ke landscape sekarang; (2) cek kontrol bawah (scrubber/waktu/tombol) kejangkau penuh, 0 ke-potong gesture-nav, baik di portrait MAUPUN landscape; (3) kalau kliping MASIH terjadi setelah fix reinforcement ini, itu sinyal root cause-nya bukan `decorFitsSystemWindows` sama sekali (butuh screenshot+logcat baru buat diagnosis lanjut, bukan tebakan lanjutan tanpa data). Batch126: **user re-report — clipping DAN rotasi TETAP gagal setelah Batch122+Batch125, kedua fix sebelumnya dari dalam Dialog** (3 file: `SwipeScreenCard.kt`, `SwipeScreenGrid.kt`, `SwipeScreen.kt`). **Root cause dikoreksi total (bukan device/OEM quirk lagi, tapi limitasi Compose yang terdokumentasi publik)**: `Dialog(usePlatformDefaultWidth=false)` buka `Window` terpisah — laporan publik dengan setup identik (GitHub compose-multiplatform discussion #4683) konfirmasi gejala PERSIS sama: `WindowInsets` di dalam Dialog itu selalu nol, "as if the Dialog is oblivious to the navigation bar", walau `decorFitsSystemWindows` sudah diset false — jadi reinforcement Batch125 (SideEffect+DialogWindowProvider) mendorong property yang SALAH, bukan akar masalahnya. Laporan publik lain dengan DialogProperties identik juga sebut breakage saat rotasi ("ui breaks sometimes") — window kedua yang terpisah dari window Activity utama (yang sudah benar configChanges+SENSOR-nya sejak Batch122) itu sendiri titik lemahnya. Fix: **Dialog/DialogProperties/DialogWindowProvider dicabut total** dari `FullscreenViewer` — sekarang overlay biasa (`Box` + `BackHandler` pengganti `onDismissRequest`) yang nge-compose LANGSUNG di window yang sama dengan seluruh app. `MainActivity` sendiri TIDAK edge-to-edge (grep project-wide: `setDecorFitsSystemWindows` 0 pernah dipanggil buat window Activity-nya) — sama seperti semua layar lain di app ini yang 0 pernah butuh windowInsetsPadding manual — jadi OS otomatis udah jaga konten ini gak nabrak status/gesture-nav bar, 0 kode insets tambahan diperlukan. **Temuan struktural penting saat verifikasi sebelum eksekusi**: call site Swipe-mode (`SwipeScreen.kt` baris ~441) sudah sibling ke `Scaffold`-nya sendiri (aman, fillMaxSize() akan genuinely full-screen), TAPI call site Grid-mode lama (`FullscreenViewer` dipanggil dari DALAM `GridSelectContent`, yang sendiri dipanggil DI DALAM content slot `Scaffold` milik `SwipeScreen.kt`) BUKAN sibling — tanpa Dialog (yang dulu selalu bikin window baru independen dari kedalaman nesting-nya), sebuah `Box` biasa di situ cuma bakal fullscreen relatif ke content slot Scaffold, bukan ke layar fisik. Fix tambahan: `GridSelectContent` (`SwipeScreenGrid.kt`) cabut `zoomedItem` state + panggilan `FullscreenViewer`-nya sendiri, ganti parameter baru `onZoomRequest: (MediaItem) -> Unit`; `SwipeScreen.kt` sekarang pegang `zoomedGridItem` (`remember(group.key)`, reset-per-group sama seperti state tetangganya) dan panggil `FullscreenViewer` SEKALI lagi di level top yang sama dengan yang sudah ada punya Swipe-mode. **Regresi kecil yang diantisipasi + ditutup di batch yang sama**: Dialog dulu otomatis "modal" (window terpisah nangkep semua tap di dalamnya) — overlay biasa 0 punya jaminan itu. Cabang foto sudah aman (`clickable` fillMaxSize()-nya sendiri), cabang video-lagi-main juga aman (PlayerView native View nangkep touch-nya sendiri) — TAPI cabang video error-state (`playbackError=true`) cuma render `Column` di tengah, area kosong di sekitarnya bisa tembus ke SwipeCard/grid di bawahnya. Ditambah 1 `Box` no-op `clickable` (`indication=null`) nutupin `matchParentSize()` di cabang itu doang — nyaman diverifikasi sempit, 0 nyentuh cabang video yang lagi main (yang emang sengaja 0 dikasih clickable sejak Batch121, biar gak rebutan tap sama kontrol PlayerView). Komentar Batch125 yang sekarang stale (rujukan ContextThemeWrapper/Dialog) DIKOREKSI di tempat (coret+catatan baru), bukan dihapus — `findActivity()` sendiri TETAP dipertahankan (tetap benar walau Dialog-nya sudah hilang, harmless). Verifikasi: brace/paren balanced ketiga file (67/67+195/195, 39/39+124/124, 120/120+209/209 — dicek python setelah seluruh edit), 0 sisa referensi `Dialog`/`DialogProperties`/`DialogWindowProvider`/`WindowCompat` di kode aktif (cuma tersisa di komentar historis + `FileInfoDialog`/`AlertDialog` yang emang Dialog lain, tidak terkait). **Stuttering video TETAP belum digarap batch ini** (scope eksplisit user: clipping+rotasi duluan, sama seperti Batch125). **Belum tervalidasi compiler/CI/device asli SAMA SEKALI** (sandbox 0 Android SDK/emulator) — ini clean-break dari pendekatan Dialog yang sudah gagal 2x, root cause sekarang didukung laporan publik (bukan tebakan device-specific lagi), TAPI tetap kemungkinan ada detail Compose version-specific yang cuma ketauan dari build/device asli. User WAJIB: (1) build + buka video, pastikan kontrol bawah (scrubber/waktu/tombol) 0 ke-potong gesture-nav baik portrait MAUPUN landscape; (2) fisik rotasi hp saat video/foto fullscreen terbuka (baik dari Swipe-mode MAUPUN Grid-mode zoom-icon), pastikan BENERAN rotasi ke landscape; (3) coba video yang gagal main (`playbackError` state) dan pastikan tap di area kosong sekitar teks error TIDAK menembus ke swipe/grid di baliknya; (4) test tombol close (video) dan tap-to-dismiss (foto) masih berfungsi normal seperti sebelumnya.

## Belum Dikerjakan (Prioritas Berikutnya)
- **🟢 [Batch136] User bug report device-test — gesture volume (kanan, Batch134) "cuman kosmetik/gak ngaruh ke master volume device sama sekali" (1 file, `SwipeScreenCard.kt`).** User implisit konfirmasi Batch131-135 SISANYA jalan normal ("semua sudah sesuai cara kerja batch sebelumnya") — install+run di device asli MEMBUKTIKAN compile+build sukses (device gak bisa jalanin APK gagal-compile), jadi STACKING WARNING 5-batch (131-135) di [RESUME POINT] lama SEKARANG resolved (compile+runtime dasar confirmed), sisa cuma 1 gap fungsional ini yang belum sempat ke-device-test detail sebelumnya. **Root cause TERKONFIRMASI baca kode langsung**: `manualVolume` (Batch134) cuma nge-feed `exoPlayer.volume` — gain internal 1 instance ExoPlayer ini doang, 0 PERNAH sentuh `AudioManager`/`STREAM_MUSIC` (volume asli device) — beda total dari `brightnessLevel` di composable yang SAMA PERSIS, yang MEMANG nulis ke `activity.window.attributes.screenBrightness` (setting fisik asli). Efek bug: geser kanan cuma meredam suara ExoPlayer RELATIF ke volume sistem yang gak pernah berubah — 0 kedengeran di luar 1 video itu, 0 persist, 0 nyambung ke tombol fisik volume hp/indicator OS. Fix: `manualVolume` sekarang baca/tulis LANGSUNG `AudioManager.STREAM_MUSIC` (`getSystemService(Context.AUDIO_SERVICE)`, `getStreamVolume`/`getStreamMaxVolume` buat init+skala, `setStreamVolume(STREAM_MUSIC, idx, flags=0)` tiap drag tick — `flags=0` sengaja biar OS volume-UI bawaan gak numpuk sama `GestureLevelIndicator` bar sendiri). `remember(uri)` DICABUT jadi `remember{}` polos (pola sama persis `brightnessLevel`) — WAJIB bukan kosmetik: kalau tetap per-uri, ganti video bakal maksa reset volume device ke nilai lama (awalnya 100%) TANPA aksi user, regresi baru yang lebih parah dari bug asalnya. `exoPlayer.volume` sekarang cuma `1f`/`0f` (ikut `isMuted`) — loudness asli sekarang murni dari stream sistem, sama seperti video player mainstream (YouTube dst). Mute button (Batch133, `manualMuteOverride`) 0 disentuh, tetap independen. `DisposableEffect` restore-brightness (`BRIGHTNESS_OVERRIDE_NONE` on close) SENGAJA 0 ditiru buat volume — beda kelas setting: brightness itu WINDOW OVERRIDE yang emang harus direstore ke sistem, volume device itu SETTING PERSISTEN asli (sama kayak user pencet tombol fisik — harus tetap nempel setelah viewer/app ditutup); restore-on-close di sini malah jadi bug baru (ngebalikin paksa volume yang user set sengaja). 0 permission baru diperlukan — `setStreamVolume` non-protected API, beda dari `MODIFY_AUDIO_SETTINGS` (itu cuma buat ubah routing/mode audio) — dicek web search ke developer.android.com sebelum ditulis, bukan asumsi. Verifikasi: brace/paren `SwipeScreenCard.kt` 148/148 + 472/472 (python, naik bersih dari Batch135's 145/145+463/463). Import baru: `android.media.AudioManager`, `kotlin.math.roundToInt`. Grep ulang `manualVolume`/`audioManager` — 0 sisa referensi pola lama (`remember(uri)`+gain-only), 4 titik pakai baru semua konsisten. **Batas jujur (ZERO-HALLUCINATION, out-of-scope fix ini)**: 0 live-listener buat perubahan volume DARI LUAR gesture (mis. user pencet tombol fisik volume pas video lagi kebuka) — `manualVolume`/indicator TIDAK auto-refresh real-time ke situ (baru resync pas composable ini re-init, mis. buka video baru), beda scope dari bug yang dilaporkan (gesture tidak berpengaruh) — kalau user butuh live-sync itu juga, perlu instruksi eksplisit terpisah (kemungkinan butuh broadcast receiver `VOLUME_CHANGED_ACTION`, di luar cap 1-file/scope laporan ini). **Belum tervalidasi compiler/CI/device asli SAMA SEKALI untuk fix ini** — user WAJIB build+install+cek: (1) geser kanan pas video main → volume MEDIA device (cek notification shade/tombol fisik hp) BENERAN naik-turun ikutan, bukan cuma bar indikator doang; (2) ganti video (swipe) sambil viewer masih kebuka → level volume TIDAK ke-reset paksa ke 100% (regresi yang justru fix ini cegah, cek poin ini secara khusus); (3) mute button (Batch133) TETAP jalan independen dari gesture ini; (4) drag brightness (kiri) MASIH normal seperti sebelumnya (0 disentuh batch ini, tapi 1 composable sama — regresi turunan paling beresiko kalau ada, sama pola warning Batch135).

- **🟢 [Batch135] hotfix regresi Batch134, user device-test + bug report langsung (1 file, `SwipeScreenCard.kt`)** — 2 temuan: (1) "absennya indicator/label mini diatas slider... user bingung ini untuk atur brightness atau volume", (2) "navigasi video setelah fadeout malah gak bisa balik nampilin. harus keluar masuk videonya dulu baru balik normal!!". **Temuan (2), ROOT CAUSE TERKONFIRMASI** (bukan dugaan — dicek langsung ke dokumentasi resmi `developer.android.com/.../understand-gestures`, bagian "Event dispatching and hit-testing"): asumsi Batch134 sendiri SALAH — komentar lama klaim "tap biasa (tanpa consume) jatuh tembus ke `clickable` `AndroidView` di bawahnya" TIDAK BEGITU cara kerja Compose. Kutipan resmi: *"when there are multiple eligible composables on the same level of the tree, only the composable with the highest z-index is hit... Composables that are not in the chain never receive pointer events, even when the pointer is inside of their bounds."* Begitu gesture-zone `Row` (Batch134) nutupin `AndroidView` secara z-order, `AndroidView`'s `clickable` JADI DEAD CODE — 0 pernah dapat event apapun lagi utk area itu, tap maupun drag, consumed atau tidak — persis kenapa nav gak pernah balik kecuali keluar-masuk video (yang reset `controlsVisible` via `remember(item.uri)`). Fix: `clickable` lama di `AndroidView` DICABUT (memang sudah gak guna), tap-to-toggle dipindah LANGSUNG ke kedua gesture-zone `Box` via `detectTapGestures` di `.pointerInput` KEDUA yang TERPISAH dari `.pointerInput` drag yang sudah ada — persis pola yang direkomendasikan RESMI di dokumen yang sama ("use separate pointerInput modifier instances instead", disertai contoh kode identik: `detectTapGestures` + `detectDragGestures` di 2 `.pointerInput(Unit)` beruntun di 1 composable, dikonfirmasi "will correctly be triggered"). **Bug turunan yang KETAHUAN & langsung ditutup saat verifikasi fix ini** (bukan dari laporan user, ditemukan sendiri saat baca ulang sebelum eksekusi): `onTap = { onControlsVisibleChange(!controlsVisible) }` kalau ditulis naive bakal baca `controlsVisible` yang STALE — `.pointerInput(uri)` di-key ke `uri` doang (bukan `controlsVisible`), jadi coroutine-nya long-lived lintas tiap toggle/auto-hide, closure-nya bakal "beku" di nilai `controlsVisible` pas coroutine itu pertama kali jalan (classic Compose stale-closure gotcha) — DIBEDAKAN dari drag-handler yang sudah ada (`manualMuteOverride`/`brightnessLevel`/`manualVolume`, semua `var...by remember{mutableStateOf}` — itu AMAN dari masalah ini krn baca/tulisnya lewat State object yang stabil, bukan parameter mentah). Fix: `val currentControlsVisible by rememberUpdatedState(controlsVisible)` — dipakai HANYA di dalam closure tap-handler itu, sisanya (LaunchedEffect/AnimatedVisibility yang sudah ada) tetap baca `controlsVisible` mentah seperti biasa (sudah benar, gak kena masalah ini krn direstart tiap kali). **Temuan (1)**: `GestureLevelIndicator` (Batch134) ditambah parameter `label: String`, dirender di ATAS bar (Text, tetap TANPA icon — disiplin sama Batch134), dipanggil dengan `"Brightness"`/`"Volume"` di kedua call site. Verifikasi: brace/paren `SwipeScreenCard.kt` 145/145+463/463 (naik bersih dari Batch134's 143/143+450/450). `rememberUpdatedState`/`detectTapGestures` dicek ada di package `androidx.compose.runtime`/`androidx.compose.foundation.gestures` (wildcard import yang sudah ada + 1 import baru), pola 2-`pointerInput`-terpisah dikonfirmasi LANGSUNG dari kode contoh resmi Google, bukan blog pihak ketiga. **Belum tervalidasi compiler/CI/device asli SAMA SEKALI** — sekarang 5 batch tertumpuk (131→135) di file paling rawan project ini, lihat STACKING WARNING + checklist device-test yang diperbarui di [RESUME POINT].

- **🟢 [Batch134] User minta langsung ("next task", setelah konfirmasi "batch progress 1-6✅" — 6 poin cek Batch133 hijau, syarat yang ditetapkan RESUME POINT batch lalu) — "fitur slider brightness/volume samping kiri-kanan pada video player like YouTube ReVanced" + "navigasi VideoPlayer wajib fadeout saat tidak digunakan" — DIEKSEKUSI TANPA MENUNGGU konfirmasi lebih lanjut, 1 file (`SwipeScreenCard.kt`).** (a) **Gesture brightness/volume**: 2 zona `detectVerticalDragGestures` (dikunci vertikal, bukan `detectDragGestures`) selebar 50/50 layar video, ditumpuk SETELAH `AndroidView` (hit-test duluan) tapi TANPA `clickable` sendiri — `change.consume()` cuma jalan pas ngelewatin touch slop, jadi tap biasa (bukan drag) tetap tembus ke `clickable` toggle-controls `AndroidView` lama, 0 berubah. Kiri = `brightnessLevel` (`WindowManager.LayoutParams.screenBrightness` di `activity.window`, baca nilai window saat ini/fallback 0.5f, TIDAK `remember(uri)` — setting fisik layar bukan per-video, reset ke `BRIGHTNESS_OVERRIDE_NONE` digabung ke `DisposableEffect(Unit)` restore-orientasi yang SUDAH ADA, bukan effect baru). Kanan = `manualVolume` (`remember(uri)`, default 1f) — diselipkan DI BAWAH `isMuted` Batch133 (`exoPlayer.volume = if (isMuted) 0f else manualVolume`, sebelumnya `else 1f`), tombol mute Batch133 0 berubah perilaku, cuma sekarang mute "level yang mana pun" bukan selalu full. Mulai drag volume = auto `manualMuteOverride = false` (unmute), pola gesture volume standar YouTube/ReVanced. Indikator: `GestureLevelIndicator` baru (persen + bar vertikal, TANPA icon — project ini punya histori 3x CI gagal gara-gara nebak nama icon Batch110/113/128, jadi indikator sengaja 0 butuh icon baru, 0 risiko itu di batch ini), auto-hide sendiri (`showBrightnessIndicator`/`showVolumeIndicator`, `false` di `onDragEnd`/`onDragCancel`) — terpisah dari timer nav (b). (b) **Auto-fadeout nav**: `controlsVisible` (dulu private `remember(uri)` di dalam `VideoPlayerSurface`) di-hoist ke `FullscreenViewer` (`remember(item.uri)`, key sama persis) supaya row Close+nama-file (sebelumnya SELALU tampil, 0 pernah ikut hide) sekarang ikut state sama dengan `VideoControlBar`. `VideoPlayerSurface` terima 2 parameter baru: `controlsVisible: Boolean` + `onControlsVisibleChange: (Boolean) -> Unit`. Timer auto-hide Batch127 (3 detik idle) TETAP 1 `LaunchedEffect` yang sama, sekarang manggil `onControlsVisibleChange(false)` bukan nulis var lokal — 0 timer kedua. Kedua row (Close+nama-file `FullscreenViewer`, transport-controls+mute+rotate `VideoControlBar`) dibungkus `AnimatedVisibility(fadeIn(tween(200))/fadeOut(tween(300)))` gantiin `if(controlsVisible){...}` polos — FADE beneran (bukan muncul/hilang instan), sesuai permintaan eksplisit user. Gesture zone (a) SENGAJA tidak ikut mati saat nav faded-out (selalu aktif, sama pola player mainstream). Verifikasi: brace/paren `SwipeScreenCard.kt` 143/143+450/450 (python, naik bersih dari baseline Batch133 117/117+354/354 — murni penambahan). Semua API baru (`detectVerticalDragGestures`, `AnimatedVisibility`/`fadeIn`/`fadeOut` dari `androidx.compose.animation`, `PointerInputScope.size`, `WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE`) dicek via web search sebelum dipakai, bukan tebakan — disiplin sama Batch133 (icon). 0 dependency baru, `strings.xml` 0 disentuh. **Belum tervalidasi compiler/CI/device asli SAMA SEKALI** — lihat STACKING WARNING + checklist device-test 7-poin di [RESUME POINT].
- **🟢 [Batch133] User minta langsung ("next task") — "lengkapi navigasi... tambahkan toggle audio dan rotation mode didalam video player like basic apps" — DIEKSEKUSI TANPA MENUNGGU konfirmasi CI Batch131/132 (lihat catatan STABILITY WINS di [RESUME POINT] batch lalu: "tunggu dulu SEBELUM lanjut ke apapun lagi, termasuk Fase E item baru"). Ini bukan Fase E, tapi 1 file yang sama (`SwipeScreenCard.kt`) yang juga disentuh Batch132 (belum confirmed) DAN yang paling sering regresi di seluruh histori project (Batch121→128, minimal 5 ronde bug/hotfix) — jadi risiko stacking di atas base yang belum tervalidasi ini nyata, dieksekusi karena user secara eksplisit kasih instruksi langsung (P1), bukan diasumsikan aman. 1 file (`SwipeScreenCard.kt`), 0 file lain disentuh.** Root cause/gap: (1) audio — app SUDAH punya setting global "Sound for Videos" (Settings), TAPI 0 cara cepat mute/unmute video yang lagi dibuka tanpa keluar dulu ke Settings; (2) rotasi — `SCREEN_ORIENTATION_SENSOR` (Batch122) sudah bikin video ikut fisik-rotasi hp, TAPI 0 tombol manual buat maksa landscape tanpa benar-benar memiringkan hp (skenario: auto-rotate sistem lagi off, atau user gak mau miringkan hp). Kedua gap ini genuinely belum ada sebelum batch ini, dikonfirmasi baca `VideoControlBar`/`VideoPlayerSurface` langsung, bukan tebakan. Fix, `VideoControlBar`'s transport-controls row diubah dari `Arrangement.Center` (cuma rewind/play/forward) jadi `Arrangement.SpaceBetween` 3-zona: ikon mute (`Icons.Filled.VolumeUp`/`VolumeOff`) di kiri, transport controls tetap ter-grup di tengah (nested `Row`), ikon rotasi (`Icons.Filled.ScreenRotation`/`ScreenLockRotation`) di kanan — pola 3-zona umum di pemutar video "basic apps" (bukan klaim ini cocok persis sama Sponge — `ROADMAP.md`'s catatan Sponge cuma soal Settings/Cleaning-Options/Home-dashboard, BUKAN layout kontrol video-nya, jadi tidak dicek-silang ke situ). State baru: `manualMuteOverride: Boolean?` (`remember(uri)`, null = ikut default Settings persis kayak sebelum batch ini — TIDAK menyentuh fix Batch132 sama sekali, cuma nambah layer yang bereaksi ke hasilnya) — `isMuted = manualMuteOverride ?: !videoSoundEnabled`, dipakai `LaunchedEffect(exoPlayer, isMuted)` gantiin `LaunchedEffect(exoPlayer, videoSoundEnabled)` lama. Tap tombol mute di player = override sesi ini doang (video berikutnya balik ikut default Settings, sama kayak tombol mute di pemutar video pada umumnya — bukan permanent, sengaja TIDAK ditulis balik ke DataStore, biar tetap 1 file/0 nyentuh `SettingsStore.kt`). Rotasi: `manualLandscape: Boolean` (`remember(uri)`, default false) — `LaunchedEffect(manualLandscape)` nge-set `requestedOrientation` ke `SCREEN_ORIENTATION_SENSOR_LANDSCAPE` (true)/`SENSOR` (false, behavior lama Batch122 persis). Capture+restore orientasi sebelum video dibuka (`previousOrientation`) DIPISAH dari effect yang nentuin orientasi aktif — dulu 1 `DisposableEffect(Unit)` ngerjain keduanya; kalau tetap digabung, re-run tiap toggle rotasi bakal salah nyimpen "previous" (bisa kecapture SENSOR/SENSOR_LANDSCAPE dari toggle sebelumnya, bukan orientasi asli sebelum video dibuka sama sekali) — sekarang `previousOrientation` cuma `remember{}` (capture sekali, timing sama kayak sebelumnya, sebelum effect apa pun jalan) + `DisposableEffect(Unit)` yang cuma restore pas viewer beneran ditutup. Icon: `VolumeUp`/`VolumeOff` dan `ScreenRotation`/`ScreenLockRotation` dicek dulu sebelum ditulis (bukan tebakan — project ini punya histori 3x CI gagal gara-gara nebak icon, Batch110/113/128) — web search konfirmasi keduanya nama icon Material asli (`screen_rotation`/`screen_lock_rotation` ada di `google/material-design-icons` codepoints resmi; `VolumeUp`/`VolumeOff` sempat jadi kandidat auto-mirrored sejak `material-*:1.6.0-alpha05`, TAPI versi lama non-mirrored-nya cuma di-deprecate bukan dihapus di rilis yang sama — project ini pin `compose-bom 2024.06.00` yang notabene SETELAH rilis itu, jadi `Icons.Filled.VolumeUp`/`VolumeOff` dipastikan masih ada, walau kemungkinan muncul warning deprecated, bukan error compile). `material-icons-extended` masih aktif dari Batch128 (rule#5 proguard blanket-cover package `filled`) jadi 0 risiko "genuinely extended-only" kelas Batch110/128 buat ke-4 icon baru ini, dan 0 proguard-rules.pro baru diperlukan. Verifikasi: brace/paren `SwipeScreenCard.kt` 117/117+354/354 (python, dicek ulang setelah seluruh edit — naik dari baseline Batch132's 105/105+324/324 karena penambahan bersih, bukan makro-refactor). `strings.xml` TIDAK disentuh — 4 content-description baru ("Mute"/"Unmute"/"Rotate to landscape"/"Switch to auto-rotate") tetap literal inline, KONSISTEN sama precedent yang SUDAH ADA di file ini sendiri sejak Batch127 ("Rewind 10 seconds"/"Play"/"Pause"/"Forward 10 seconds" juga literal, tidak pernah di-`stringResource()`-kan meski P2 #11 Stage 4/Batch71 sudah nutup gap serupa di file lain — gap ini pre-existing khusus file ini, di luar scope batch ini buat sekalian dibenerin). `CHANGELOG.md` diupdate (entry Batch133, append di atas Batch132 sesuai urutan newest-first file itu). **Belum tervalidasi SAMA SEKALI** (sandbox 0 Android SDK/compiler/device, sama limitasi standar tiap batch) — DAN beda dari batch2 biasa, ini numpuk di atas 2 batch YANG JUGA belum tervalidasi (131 rotasi-controls-nya baseline, 132 fix defaultnya) di file paling rawan regresi di seluruh project. User WAJIB push + build, LALU device-test SEMUA ini sekaligus (bukan cuma yang baru): (1) video tetap otomatis ada suara tanpa buka Settings dulu (Batch132's fix); (2) share-text toggle masih jalan normal (Batch131, file lain, harusnya 0 kesenggol tapi belum re-check); (3) tombol mute BARU — tap sekali langsung bisu, tap lagi langsung ada suara lagi, dan video BERIKUTNYA (bukan yang lagi dibuka) balik ke default Settings; (4) tombol rotasi BARU — tap sekali maksa landscape TANPA fisik miringkan hp, tap lagi balik ke mode ikut-sensor (Batch122's behavior lama); (5) fisik miringkan hp SAAT mode rotasi manual masih aktif — pastikan tetap landscape (bukan malah balik portrait, itu tandanya `SENSOR_LANDSCAPE` salah diterapkan); (6) tutup video, buka lagi — pastikan orientasi hp balik ke sebelum video dibuka (bukan nyangkut landscape). Kalau ADA yang gagal dari 6 poin ini, cek dulu apakah gara-gara Batch133 (baru) atau warisan 131/132 (lama, belum pernah teruji) — jangan asumsikan otomatis Batch133 biang keroknya.
- **🟢 [Batch132] Regresi audio, SALAH Claude SENDIRI dari Batch129 — user report "fix regression pada output Audio!!", 0 log/screenshot dilampirkan, TAPI diagnosis PASTI (bukan tebakan) dari re-baca kode sendiri.** Root cause: sebelum Batch129, `VideoPlayerSurface` (`SwipeScreenCard.kt`) 0 pernah nge-set `exoPlayer.volume` sama sekali — artinya ExoPlayer pakai default bawaannya sendiri, `1f` (suara nyala penuh), SELALU. Batch129 nambahin toggle "Sound for Videos" dan bikin defaultnya **OFF** (alasan waktu itu: "public transport, ruang tenang" — asumsi Claude sendiri, BUKAN instruksi user) — efeknya: SEMUA video yang tadinya otomatis ada suara jadi BISU secara diam-diam buat siapapun yang belum pernah buka Settings. Ini REGRESI BENERAN (fitur yang tadinya jalan jadi rusak), bukan cuma "pilihan default yang bisa didebat" — dikonfirmasi grep `\.volume\s*=` di SELURUH project cuma nemu 1 titik (baris yang sama persis Batch129 tambahin), jadi 0 kemungkinan ada regresi audio LAIN yang kelewat. **Fix, 4 file** (semuanya cuma ganti nilai default, 0 logic baru): `SettingsStore.kt` (`?: false` → `?: true` di `videoSoundEnabledFlow`, doc comment ditulis ulang total — akui alasan Batch129 salah, bukan cuma diem-diem diganti), `SettingsScreen.kt` (`collectAsState(initial = false)` → `initial = true`, biar switch 0 sempet keklip "off" 1 frame padahal default asli "on"), `SwipeScreenCard.kt` (`collectAsState(initial = false)` → `initial = true` — INI bagian paling penting dari fix, krn `ExoPlayer.Builder().build().apply{}` baca `videoSoundEnabled` di komposisi PERTAMA, SEBELUM nilai asli dari DataStore sempat nyampe — `initial=false` yang lama bikin player literally KEBANGUN bisu dulu baru (mungkin) dibenerin belakangan, bukan cuma UI yang salah tampil), `strings.xml` (subtitle "...instead of muted by default" sekarang KEBALIK dari kenyataan, diganti "Turn off to review videos muted instead of with sound."). **Sekarang match Sponge** (rekaman Batch129 nunjukkin toggle-nya Sponge ON by default) — kebetulan, bukan sengaja niru, murni krn assumption Claude soal "audio mengganggu" yang salah dan Sponge's real-world default ternyata benar. Verifikasi: brace/paren semua file OK (`SwipeScreenCard.kt` 105/105+324/324, `SettingsStore.kt` 55/55+63/63, `SettingsScreen.kt` 163/163+454/454), `strings.xml` valid XML, grep ulang `videoSoundEnabled.*false` di seluruh `.kt` project = 0 hasil (bersih, 0 sisa default lama kecawa nyantol). `ROADMAP.md` Section 2b dikoreksi (klaim "default OFF sengaja beda dari Sponge" dihapus, ganti jujur ini bekas regresi). **Belum tervalidasi CI/device** — user WAJIB push+cek run berikutnya, IDEALNYA device-test langsung: buka video, pastikan ada suara TANPA buka Settings dulu (itu skenario yang regresi kemarin).
- **🟢 [Batch131] User konfirmasi CI hijau ("hijau, next") + minta lanjut — dipilih sendiri item Fase E berikutnya (user udah kasih izin lanjut otonom sebelumnya, "oke di-split beberapa batch", 0 ditanya ulang soal urutan supaya gak keliatan gak progresif).** Item ke-2 dari "Cleaning Options" (`ROADMAP.md` #14): **"Enable share text"** — dipilih karena paling ringan/mandiri dari 7 sisa (1 file consumer, `SwipeScreen.kt`, 0 nyentuh logic inti swipe kayak swipe-direction/manage-albums). 4 file: `SettingsStore.kt` (+`shareTextEnabledFlow`/setter, pola identik `videoSoundEnabledFlow` Batch129), `SettingsScreen.kt` (+1 toggle row di section "Cleaning Options" yang udah ada dari Batch129, bukan section baru), `strings.xml` (+2 string), `SwipeScreen.kt` (baca setting via `SettingsStore` lokal — pola sama `VideoPlayerSurface` — nambahin `Intent.EXTRA_TEXT` = `currentItem.displayName` ke share intent kalau enabled). Default **OFF** (beda alasan dari Sound-for-Videos: bukan soal "mengganggu", tapi nama file hasil rename sendiri bisa bocorin info personal ke penerima share — opt-in lebih aman). Verifikasi: brace/paren `SwipeScreen.kt` 122/122+214/214, `SettingsStore.kt` 55/55+63/63, `SettingsScreen.kt` 163/163+452/452, `strings.xml` valid XML. `ROADMAP.md` direstruktur: item 14 "Sound for Videos" (dulu item terpisah) + "8 sisa" digabung jadi 1 checklist 9-sub-item biar gak ambigu lagi mana yang udah/belum (2/9 selesai sekarang). **Belum tervalidasi CI/device untuk PERUBAHAN INI** (beda dari Batch128-130 yang user udah eksplisit konfirmasi hijau) — user WAJIB push+cek run berikutnya sebelum lanjut batch berikutnya lagi.
- **🟢 [Batch130] CI infra hotfix — user upload `logs_94523079153.zip` (bukan `log-fail_...` seperti Batch128, ini "download log archive" penuh dari GitHub UI, semua step ke-zip) + minta "path khusus... label dinamis" utk artifact log-fail. 1 file: `.github/workflows/build.yml`.** Diagnosis dari `0_build.txt` (log gabungan job): run kali ini gagal di step **"Set up Android SDK"** (`android-actions/setup-android@v3`) — SAMA SEKALI BUKAN soal kode Kotlin (Batch128's fix kemungkinan besar sudah benar, belum sempat kebuktiin krn CI mati duluan di step SEBELUM Gradle jalan). Root cause (dikonfirmasi web search, kejadian SAMA di banyak project lain tanggal SAMA — 2026-09-14): Google mencabut package legacy `'tools'` dari repository SDK-nya; `setup-android@v3` defaultnya minta `packages: 'tools platform-tools'` kalau 0 di-override, jadi `sdkmanager` nyoba fetch `'tools'` yang udah dihapus → exit code 1 → step mati SEBELUM Gradle sempat jalan. Fix: `packages: ''` di step itu (skip install bawaan action-nya) — aman krn step SESUDAHNYA ("Install Android SDK components") udah eksplisit install `platform-tools`+`platforms;android-35`+`build-tools;35.0.0` sendiri, 0 pernah gantung ke default action ini. **Bug KEDUA, ini yang user minta eksplisit**: step "Upload build failure log" CRASH juga (`##[error]Input required and not supplied: path`) — `LOG_FILE` cuma di-set DI DALAM step "Build signed release APK", yang di run ini 0 sempat jalan (mati duluan di SDK setup) → `${{ env.LOG_FILE }}` resolve ke STRING KOSONG → `actions/upload-artifact@v4` treat itu FATAL (beda dari "file gak ketemu" yang udah di-handle `if-no-files-found: warn`). Fix: pindahin komputasi `LOG_FILE` ke step baru **"Compute failure-log path"** PALING AWAL (persis setelah Checkout, sebelum apapun bisa gagal) — namanya TETAP dinamis sama persis kayak sebelumnya (`log-fail_<branch>_run<N>-attempt<M>_<sha>.log`, cuma waktu komputasinya yang dipindah, bukan formatnya), jadi `LOG_FILE` SELALU ada nilai valid sepanjang job apapun titik gagalnya — kalau gagal SEBELUM step Gradle (jadi 0 ada file fisik di path itu), `if-no-files-found: warn` yang udah ada dari dulu bakal nge-warn doang (bukan crash), krn sekarang variabelnya sendiri 0 pernah kosong. Step "Build signed release APK" TETAP `tee "$LOG_FILE"` (0 berubah logic-nya, cuma 3 baris computasi lama dihapus krn udah redundant). Verifikasi: `python3 -c "import yaml; yaml.safe_load(...)"` OK, urutan step re-cek manual (`Checkout → Compute failure-log path → Set up JDK → Set up Android SDK (packages='') → ...`), `packages: ''` dikonfirmasi parse sebagai empty-string (bukan `null`). **Belum tervalidasi CI asli** — 3 hal harus dicek user abis push: (1) "Set up Android SDK" step lolos, (2) kalau Gradle-nya SENDIRI masih gagal (misal icon issue Batch128 ternyata belum kelar), file `log-fail_*.log` beneran ke-upload dgn isi yang bener, (3) kalau gagal SEBELUM Gradle lagi (skenario lain), step Upload cuma warn, GAK crash kedua kalinya.
- **🟢 [Batch129] Fase E dibuka — gap fungsional vs "Sponge" ditemukan dari screen recording user (72 detik, 2026-09-14), bukan cuma soal video player seperti awalnya dikira Batch128's follow-up.** User awalnya komplain screenshot video player ("something is missing"), Claude salah tebak 3x (duplicate controls / not-fullscreen / blank icon — SEMUA salah, screenshot asli ternyata NORMAL setelah dicek ulang pakai `view` langsung ke file, bukan dari deskripsi). User lalu upload rekaman 72 detik app kompetitor **"Sponge"** (bukan app kita!) — ternyata itu app yang SUDAH jadi tolok ukur resmi project ini sejak awal (`ROADMAP.md`, judul filenya sendiri "Snaply vs Sponge"), dan `ROADMAP.md` SENDIRI eksplisit bilang "Cek ulang sebelum rilis besar — fitur kompetitor bisa berubah" — rekaman ini ADALAH re-cek itu, ketemu banyak fitur Sponge yang 0 tercatat di `ROADMAP.md` lama (lihat `ROADMAP.md` Section 1b/2b/Fase E, ditulis ulang Batch129). Frame-by-frame diverifikasi dari file asli (`ffmpeg` extract 1fps + `montage` contact sheet 6x6, 2 lembar, di-`view` langsung — BUKAN ditebak dari nama file/asumsi). Ke-7 gap baru: History screen (statistik bulanan, data-layer belum ada), 8 item "Cleaning Options" di Settings (baru 1/8 selesai — lihat di bawah), Home dashboard model streak/resume-card (`HomeScreen.kt` project ini perannya beda, folder browser bukan dashboard), "Customize view" bottom sheet di layar swipe, Notifications split 2 toggle, Settings > My Account (auth — prioritas PALING RENDAH, butuh keputusan produk eksplisit, JANGAN dikerjakan tanpa itu). **Batch129 sendiri (1 dari 7 item Fase E, SELESAI)**: setting **"Sound for Videos"** — 4 file (`SettingsStore.kt` +key/flow/setter, `SettingsScreen.kt` +section "Cleaning Options" section baru +1 toggle, `strings.xml` +3 string companion, `SwipeScreenCard.kt` — `VideoPlayerSurface` construct `SettingsStore` lokal lewat `LocalContext`, `collectAsState`, apply ke `exoPlayer.volume` di 2 tempat: init block DAN `LaunchedEffect(exoPlayer, videoSoundEnabled)` biar reaktif kalau setting diubah pas video lagi kebuka). Default **OFF** (beda dari Sponge yang ON di rekaman) — keputusan sengaja, konsisten pola off-by-default project ini utk toggle yang "mengganggu" kalau nyala tanpa diminta (sama kayak `cleaningReminderEnabledFlow`/`backupBeforeDeleteEnabledFlow`), BUKAN niru Sponge 1:1 buta. **Bonus fix (nemu pas re-view sebelum lanjut kerja, SOP: re-view sebelum edit lanjutan)**: `import androidx.compose.ui.text.style.TextOverflow` KETINGGALAN dari edit title-bar sebelumnya (turn video-title-fix, sebelum Batch129) — dipakai (`TextOverflow.Ellipsis`) tapi 0 pernah di-import, COMPILE-BREAKING kalau kelewat, ketauan sebelum sempat di-zip/push. Verifikasi: brace/paren `SwipeScreenCard.kt` 104/104+321/321, `SettingsStore.kt` 52/52+57/57, `SettingsScreen.kt` 158/158+438/438, `strings.xml` valid XML (dicek `xml.etree.ElementTree`). **6 item Fase E sisa BELUM digarap** — user eksplisit oke di-split beberapa batch (bukan sekaligus), urutan belum ditentukan user, JANGAN diasumsikan sistem. Belum tervalidasi compiler/CI/device asli.
- **🟡 Video stuttering (dilaporkan user Batch125 & re-konfirmasi Batch126, playback jalan tapi stuttering parah)** — SENGAJA belum digarap (user eksplisit prioritaskan rotate+kliping duluan di kedua laporan, lalu custom controller di Batch127 — ketiganya scope terpisah dari stuttering). Butuh profiling asli buat diagnosis (bukan tebakan): kemungkinan kandidat umum decode-side (bitrate/resolusi video vs kemampuan decoder device, sama kelas isu `NO_EXCEEDS_CAPABILITIES` Batch123 tapi utk smooth-playback bukan cuma bisa-play/tidak) ATAU render-side (`PlayerView` di dalam `AndroidView`/Compose recomposition overhead) — 0 bisa dibedakan tanpa profiler/logcat asli. Nunggu instruksi eksplisit user + idealnya sample video/device info buat lanjut.
- **🟡 Fullscreen viewer clipping + rotasi (Batch122→Batch125→Batch126, Dialog dicabut total)** — root cause dikoreksi dari device-quirk jadi limitasi Compose Dialog terdokumentasi (WindowInsets selalu nol + breakage saat rotasi di window terpisah), fix: `FullscreenViewer` jadi overlay biasa di window yang sama (3 file: `SwipeScreenCard.kt`+`SwipeScreenGrid.kt`+`SwipeScreen.kt`). Detail lengkap: entry Batch126 di "Versi Saat Ini". **Belum tervalidasi compiler/CI/device asli SAMA SEKALI** — user WAJIB build + re-test clipping/rotasi/tap-passthrough sebelum ini dianggap selesai (percobaan ke-3 buat bug yang sama, 2 sebelumnya dari dalam Dialog terbukti tidak cukup). Batch127: **user request — video player custom sendiri, bukan lagi kontroller bawaan media3-ui yang "generik dan penuh keterbatasan"** (1 file, `SwipeScreenCard.kt`). `PlayerView` sekarang `useController = false` — kontrol bawaan (XML `exo_player_control_view.xml`, cuma bisa di-tweak lewat attrs/style yang disediakan library) diganti kontrol penuh Compose: play/pause (dgn auto-replay-dari-0 kalau ditekan pas video sudah `STATE_ENDED`, bukan no-op di ujung mati), rewind/forward 10 detik, seek bar (`Slider` M3, drag-to-scrub + tap-di-track bawaan), label posisi/durasi (`m:ss`), indikator buffering (`CircularProgressIndicator`) saat `STATE_BUFFERING`, dan auto-hide kontrol setelah 3 detik idle (tap di video toggle balik) — SEMUA state (`isPlaying`/`isBuffering`/`durationMs`) didorong dari `Player.Listener` yang SUDAH ADA (ditambah 2 override baru ke listener yang sama, bukan listener kedua), `positionMs` di-poll tiap 300ms (satu-satunya nilai yang ExoPlayer 0 punya callback perubahannya sendiri) via `LaunchedEffect(exoPlayer)` yang otomatis ke-cancel saat composable ini keluar dari composition — sama jaminan lifecycle yang sudah dipakai `DisposableEffect` player itu sendiri. `isSeeking` flag cegah loop poll dan gesture drag rebutan nulis `positionMs` di tick yang sama (thumb bisa lompat/gemetar di bawah jari kalau dibiarkan). Tap-to-toggle kontrol sekarang aman dipasang di `AndroidView`-nya (dulu sengaja dihindari — komentar lama bilang PlayerView sendiri yang "makan" gesture tap buat toggle kontrol bawaannya — sekarang gesture itu tidak lagi ada karena `useController=false`, jadi 0 konflik). **0 icon baru yang butuh proguard-rules.pro tambahan**: `Pause`/`PlayArrow`/`Replay10`/`Forward10` semua di package `androidx.compose.material.icons.filled`, sudah ke-cover rule#5 (Batch113, `-keep class androidx.compose.material.icons.filled.** { *; }` — blanket ke SELURUH package filled, bukan cuma 7 icon yang dulu dimigrasi, dikonfirmasi baca ulang file `proguard-rules.pro` sebelum eksekusi, bukan asumsi). **Kontrol sengaja TIDAK ikut branching per-`AppTheme`/`MaterialStyle`** (putih polos di atas gradient hitam, sama seperti tombol close + teks error yang sudah ada di viewer ini sejak awal, 0 dari keduanya pernah ikut Glass/Neumorph/Cupertino/Painted — batch ini ikutin presedan yang sudah ada, bukan bikin presedan baru yang tidak diminta). **Stuttering video TETAP belum digarap/diklaim batch ini** — kontrol custom ini soal UI controller, bukan decode/render pipeline yang jadi kandidat penyebab stuttering (tetap perlu profiling asli, lihat entry "Belum Dikerjakan"). Verifikasi: brace/paren balanced `SwipeScreenCard.kt` (101/101, 303/303, dicek python setelah seluruh edit). **Belum tervalidasi compiler/CI/device asli** — user WAJIB build + tes: (1) play/pause termasuk replay setelah video habis; (2) rewind/forward 10 detik; (3) drag seek bar sambil video jalan, pastikan thumb tidak lompat-lompat; (4) tap video buat toggle kontrol muncul/hilang, dan kontrol auto-hide sendiri setelah ~3 detik saat video sedang main; (5) indikator buffering muncul saat video masih loading. **[Batch128] CI GAGAL run247, sudah di-hotfix (1 file, `app/build.gradle.kts`)**: user upload log CI gagal, `compileReleaseKotlin`, 6 error persis 3 nama — `Forward10`/`Pause`/`Replay10` (`SwipeScreenCard.kt` baris 20/21/23 import + 688/692/699 usage, titik kontrol video Batch127 di atas), 11 icon `-core` lain di file sama (`Close`/`PlayArrow`) 0 error. Root cause: klaim Batch127 "0 icon baru yang butuh proguard-rules.pro tambahan... sudah ke-cover rule#5" SALAH — nyampur 2 layer beda: rule#5 itu R8/keep-rule (relevan cuma SETELAH kompilasi sukses, fase shrink), `Unresolved reference` itu error compile-time (soal ada/tidaknya class di classpath) — 0 hubungan dgn isi proguard-rules.pro. Classpath MEMANG 0 punya ketiga class ini sejak Batch114 (`material-icons-extended` dicabut permanen) — Batch127 luput cek dependency list `app/build.gradle.kts`, cuma cek proguard-rules.pro. Fix: `material-icons-extended` DIKEMBALIKAN — pola mitigasi identik Batch109→110 (icon baru genuinely extended-only → revert dependency, tercepat & teraman). `proguard-rules.pro` rule#5 0 disentuh (sudah blanket-cover SELURUH package `filled`, termasuk extended, 0 rule baru perlu). Komentar lama Batch114 di `app/build.gradle.kts` DIKOREKSI di tempat (bukan dihapus) — "DICABUT lagi" (final) jadi "sempat DICABUT". **Efek samping jujur**: app-size gap yang ditutup Batch108-114 REOPEN — dependency ~2000+ icon balik, DAN rule#5 (blanket keep package `filled`) bikin R8 TIDAK BISA shrink package ini walau minify tetap ON, jadi app-size kemungkinan balik ke level pre-Batch114 utk seluruh icon (bukan cuma 3 icon baru ini). Verifikasi: brace/paren `app/build.gradle.kts` 22/22+95/95; `SwipeScreenCard.kt` 0 disentuh, dikonfirmasi ulang tetap 101/101+303/303 persis Batch127. **Migrasi 3 icon ini ke vector drawable lokal (pola Batch112/114) BELUM dieksekusi** — beda kelas risiko dari 7 icon Batch112 (shape geometris murni): `Replay10`/`Forward10` compound (arc panah + digit "10" dibakar ke pathData), reproduksi dari memori jauh lebih tidak pasti (`Pause` sendiri trivial, 2 batang vertikal — kalau nanti dieksekusi ini bagian resiko-terendah). Nunggu instruksi eksplisit user: lanjut migrasi demi app-size, atau terima app-size naik dikit demi kontrol video custom yang sudah diminta. **Belum tervalidasi CI asli** (sandbox 0 network) — user WAJIB push ulang, konfirmasi `run248`(+) lolos `compileReleaseKotlin`, BARU LANJUT device-test 5 poin kontrol video Batch127 di atas (belum sempat tervalidasi karena CI keburu gagal duluan).
- **🟡 Sage Wash (4th color style, Batch119+120) — compile TERKONFIRMASI OK (screenshot device asli), palette DIREVISI Batch120, belum re-konfirmasi visual device pasca-revisi** — Batch119 wiring (11 file) proven compile+render sukses (user screenshot: picker + Home screen keduanya render tema baru, brush-stroke edge kelihatan). User feedback: v1 palette kebaca "kayak app scam", bukan premium — Batch120 revisi warna (`PaintedTokens.kt`+`PaintedSurface.kt`, base digeser dingin+gelap, accent Terracotta digeser dari oranye ke brick-rose, wash alpha dinaikkan). Detail lengkap: entry Batch119 (compile confirm) + Batch120 (revisi) di "Versi Saat Ini" atas. **Nunggu user build ulang + screenshot lagi** buat konfirmasi revisi ini beneran nutup kesan "scam"-nya.
- **✅ Rotasi hp saat review "Blurry photos"/"Similar photos"/"Duplicate files"/"Search results" balik ke Home — SELESAI SEBAGAI EFEK SAMPING (Batch122)**. Bullet ini sebelumnya (Batch117) menunggu 2 pekerjaan terpisah (re-trigger scan otomatis + lift search state ke `MainActivity`) karena akar masalahnya dianggap "state hilang pas Activity recreate". Batch122 (fix rotasi video player, lihat entry-nya di "Versi Saat Ini") menghapus PREMIS itu sendiri — `MainActivity` sekarang `android:configChanges="orientation|screenSize|screenLayout"`, jadi Activity TIDAK PERNAH destroy+recreate lagi gara-gara rotasi (app-wide, bukan cuma video) — `blurryScanState`/`nearDupScanState`/`duplicateScanState`/search query+hasil (semua `remember{}` biasa, TIDAK diubah jadi `rememberSaveable`) sekarang otomatis survive rotasi karena compositionnya sendiri tidak pernah mati, bukan karena di-saveable-kan. **Belum tervalidasi device asli** (sandbox 0 SDK/emulator) — user diminta coba scan Blurry/Similar/Duplicate atau Search lalu rotasi hp fisik, pastikan tidak lagi balik ke Home.
- **🟡 App size membengkak (Batch108 investigasi → Batch109 Opsi A gagal → Batch110 revert → Batch111 Opsi B dieksekusi → Batch112 Opsi (b) Stage 1 → Batch113 hotfix regresi Batch111 → Batch114 Opsi (b) Stage 2 SELESAI, dependency dicabut → [KOREKSI Batch128] dependency DIKEMBALIKAN lagi, hotfix CI run247)** — status turun lagi ke 🟡 dari sisi dependency: `material-icons-extended` sempat 0 sejak Batch114, TAPI Batch127 (kontrol video custom) pakai 3 icon baru (`Pause`/`Replay10`/`Forward10`) yang genuinely extended-only, CI (`run247`) gagal, Batch128 kembalikan dependency (pola sama Batch109→110). R8 minify+shrink (root cause #1) TETAP aktif sejak Batch111 — TAPI rule#5 proguard (blanket keep package `filled`) berarti shrink 0 berlaku ke icon dependency ini lagi, jadi app-size icon kemungkinan balik ke level pre-Batch114. Detail lengkap: entry Batch128 di atas. Sandbox tetap 0 compiler/SDK/APK asli sepanjang investigasi ini, jadi (1) belum ada angka MB pasti sebelum/sesudah/sekarang di titik manapun, (2) 7 drawable lokal Batch112/114 TETAP dipertahankan (masih dipakai 7 titik lama, 0 di-revert — cuma dependency-nya yang balik, bukan migrasinya), (3) 7 icon hasil redraw itu tetap belum di-visual-QA di device asli. Detail per-root-cause di bawah:
  1. **R8 mati total** — `isMinifyEnabled = false` DAN `isShrinkResources = false` (`app/build.gradle.kts` baris 88-89, protected file). 0 dead-code elimination dari SELURUH dependency (bukan cuma kode sendiri), 0 resource shrinking. Ini structural root cause yang bikin poin 2 separah itu. Sudah pernah di-flag AUDIT_GAP P2 #16 (Batch92, sudut "code quality") — Batch108 mengonfirmasi ini juga akar gejala "app size", bukan cuma soal kerapian kode.
  2. **`androidx.compose.material:material-icons-extended`** (`app/build.gradle.kts` baris 135) — deskripsi resmi Maven-nya sendiri: "contains all Material icons, very large dependency, should not be included directly" (~2000+ icon; laporan publik developer lain: +8MB app size / +20MB APK size cuma dari dependency ini gak di-shrink). Grep project-wide: app INI CUMA PAKAI **18 icon unik** (angka "24" di draft awal Batch108 SALAH HITUNG, dikoreksi Batch109 — daftar nama icon di bawah ini dari awal sudah benar & lengkap), semua `Icons.Filled.*` (Lock/Close/Refresh/Search/Settings/Shuffle/Edit/Folder/PlayArrow/ArrowBack/Check/GridView/Info/Share/Sort/Undo/ViewCarousel/ZoomIn) — 0 pemakaian Outlined/Rounded/Sharp/TwoTone. Dokumentasi resmi Android: icon "paling umum dipakai" sudah otomatis kebawa `material-icons-core` (kecil, biasanya transitif lewat `material3`) — kemungkinan besar SEBAGIAN/SELURUH 24 icon itu sudah cukup dari situ, TAPI belum dikonfirmasi persis icon mana yang genuinely butuh `-extended` (0 compiler buat cek "Unresolved reference" kalau dependency dicopot).
  - ❌ **Opsi A — GAGAL, DI-REVERT Batch110** (dicoba Batch109, user pilih A): dependency `material-icons-extended` sempat dicabut dari `app/build.gradle.kts`, TAPI CI (`run231`, `compileReleaseKotlin`) konfirmasi 7 dari 18 icon project genuinely extended-only: `Shuffle` (`HomeScreen.kt`), `Folder` (`HomeScreenFolderRow.kt`), `GridView`/`Sort`/`Undo`/`ViewCarousel` (`SwipeScreen.kt`), `ZoomIn` (`SwipeScreenGrid.kt`) — 1 error tambahan di `SwipeScreen.kt:259` ("None of the following functions can be called") adalah cascading dari `GridView`/`ViewCarousel` unresolved di baris yang sama, BUKAN bug independen. Dependency dikembalikan Batch110 (mitigasi 1-baris-revert yang memang sudah diantisipasi sejak analisis Batch108). 11 icon sisanya (`Lock`/`Close`/`Refresh`/`Search`/`Settings`/`Edit`/`PlayArrow`/`ArrowBack`/`Check`/`Info`/`Share`) TIDAK error di log, jadi kemungkinan besar memang ada di `-core` — TAPI karena 7 lainnya butuh `-extended` juga, dependency itu WAJIB tetap ada selama salah satu dari 7 icon itu masih dipakai. **Jalur tersisa buat nutup app-size icon**: (a) Opsi B (R8 minify+shrink, lihat bawah) — nutup app-size dari sisi lain, TANPA perlu hapus dependency; atau (b) migrasi 7 icon itu ke vector drawable lokal (XML) biar `-extended` beneran bisa dicabut — scope lebih besar dari 1-baris, PERLU instruksi eksplisit user. **[Batch112] User pilih (b) — Stage 1 (foundation) DIEKSEKUSI, lihat bullet baru di bawah.** (b) berjalan PARALEL/TAMBAHAN ke Opsi B, bukan pengganti — R8 minify+shrink Batch111 tetap aktif; kalau (b) tuntas total, dependency `-extended` ikut hilang jadi app-size dapat manfaat dari 2 sisi sekaligus.
    - **Opsi B — DIEKSEKUSI Batch111** (user pilih B, eksplisit minta "ekstra protection" setelah Opsi A gagal): `isMinifyEnabled = true` + `isShrinkResources = true` dinyalain di `app/build.gradle.kts` (AUDIT_GAP P2 #16, root cause #1 dari 2). Dibarengi 4 keep-rule baru di `proguard-rules.pro` (CrashLogger line-number preservation, WorkManager Worker-subclass reflection, Biometric, OkHttp TLS-provider dontwarn) — detail lengkap + alasan tiap rule: entry Batch111 di "Versi Saat Ini". Coil (sudah ada dari sebelumnya) + DataStore (keep ditambah, sebelumnya cuma dontwarn) ikut ter-cover. **Belum tervalidasi compiler/CI/device asli** — nunggu build GitHub Actions push berikutnya, DAN disarankan user tes manual di device asli utk 4 fitur yang paling beresiko silent-failure (App Lock/biometric, Reminder notifikasi harian, Trash auto-expiry notifikasi, Update checker) karena reflection break TIDAK SELALU muncul sebagai compile error di CI — bisa lolos build tapi gagal diam-diam di runtime. **[Batch113] Regresi TERKONFIRMASI, sudah di-hotfix**: user cek device/build asli, 7 icon extended-only blank (`Shuffle`/`Folder`/`Sort`/`Undo`/`ViewCarousel`/`ZoomIn`/`GridView`), 11 icon `-core` sisanya normal — tepat gap yang di-flag "belum tervalidasi" di atas. Rule ke-5 ditambah di `proguard-rules.pro` (`-keep class androidx.compose.material.icons.filled.** { *; }`, scoped ke `filled` doang) — detail lengkap root cause: entry Batch113 di "Versi Saat Ini". Masih **pending validasi build baru** (apakah rule ke-5 ini beneran nutup blank-icon-nya) — user diminta re-check ke-7 icon itu setelah install APK hasil push berikutnya.
    - **Opsi (b) — Stage 1/2 (foundation) DIEKSEKUSI Batch112** (user pilih lanjut migrasi icon setelah Batch111): 7 vector drawable XML baru di `app/src/main/res/drawable/` — `ic_shuffle.xml`, `ic_folder.xml`, `ic_sort.xml`, `ic_undo.xml`, `ic_view_carousel.xml`, `ic_zoom_in.xml`, `ic_grid_view.xml` — masing-masing 1:1 pengganti visual utk `Icons.Filled.{Shuffle,Folder,Sort,Undo,ViewCarousel,ZoomIn,GridView}` (7 icon yang CI `run231` konfirmasi genuinely `-extended`-only, lihat Opsi A/Batch110 di atas). **0 file existing disentuh batch ini** (pola sengaja sama Batch74: fondasi 0-risiko dulu, wiring nanti — nambah drawable yang belum direferensikan manapun = 0 risiko compile). **Provenance/confidence, dibaca jujur (ZERO-HALLUCINATION)**: pathData 6 dari 7 (`shuffle`/`folder`/`sort`/`undo`/`view_carousel`/`zoom_in`) direproduksi dari memori model atas Material Symbols/Icons resmi milik Google (lisensi Apache 2.0, bebas dipakai/direproduksi) — sandbox ini 0 akses network buat fetch source asli dari `google/material-design-icons`, jadi TIDAK dijamin pixel-identik ke asset resmi. `ic_grid_view.xml` sengaja DIBEDAKAN — bukan klaim reproduksi, tapi **safe redraw** (grid 2x2 kotak sama besar polos) karena family icon ini paling ambigu buat direproduksi dari memori (`grid_view` vs `dashboard` vs `view_module` mirip tapi beda detail geometri) — flag eksplisit di komentar file-nya sendiri, tunggu QA visual sebelum dianggap final. Verifikasi yang SUDAH dilakukan: well-formed XML 7/7 (`python3 xml.dom.minidom`, sandbox 0 Android SDK jadi ini validasi maksimal yang bisa dilakukan tanpa compiler/render asli). **Belum tervalidasi**: visual render asli (Android Studio preview / device) DAN compile Kotlin (belum ada referensi ke drawable ini dari kode manapun batch ini). **✅ Stage 2 — DIEKSEKUSI Batch114 (user eksplisit minta lanjut)**: ganti 7 titik pakai `Icons.Filled.*` → `Icon(painterResource(id = R.drawable.ic_*), ...)` di 4 file + cabut `androidx.compose.material:material-icons-extended` dari `app/build.gradle.kts`, 1 commit atomik 5 file persis rencana di atas (precedent **STABILITY WINS** Batch77). Detail lengkap eksekusi + verifikasi + risiko tersisa: entry Batch114 di "Versi Saat Ini". **Belum tervalidasi compile/visual/APK-size asli** — jangan anggap final sebelum user build + cek device.
- **Dead-code cleanup pasca `minSdk` 31 (mulai Batch106, efek samping Batch105)** — grep project-wide `Build.VERSION.SDK_INT` nemu 20 site di 11 file. Constant ≤31 (`S`/`Q`/`O`, literal 28/30) sekarang SELALU true → else-branch dead. Constant >31 (`TIRAMISU`=33) TETAP relevan (minSdk 31 gak jamin ≥33) — situs ini JANGAN disentuh. Dikerjakan bertahap 1 file/batch (pola sama kayak Rotation-Survival Sweep bawah). Status:
  - ✅ **Stage 1 (Batch106)** — `HapticFeedback.kt` (2/2 site dead, dihapus) + `AndroidManifest.xml` (1 baris `WRITE_EXTERNAL_STORAGE`, dihapus). SELESAI.
  - ✅ **Stage 2 (Batch107)** — `GalleryCleanerApp.kt` (1/1 site dead — `>=28` di `newImageLoader()`, dihapus, `ImageDecoderDecoder.Factory()` jadi unconditional). SELESAI.
  - Sisa kandidat (belum dikerjakan, urutan bebas): `MainActivity.kt` (1 dari 2 site dead — `>=30` line ~801 dead; `>=33` line ~336 TETAP relevan, JANGAN disentuh), `CrashLogger.kt` (1 site `>=Q`), `BackupHelper.kt` (1 site `>=Q`), `ImageCompressor.kt` (1 site `>=30`), `MediaDataSource.kt` (2 site: `>=Q` + `>=30`), `MoveHelper.kt` (2 site: `>=30` + `>=Q`, PLUS 1 doc comment ~baris 102 yang sekarang stale — masih nyebut `WRITE_EXTERNAL_STORAGE` yang udah dihapus Batch106, ikut dibenerin pas stage-nya nanti), `SettingsScreen.kt` (2 dari 3 site dead — `<O` line ~201 dan `>=O` line ~255 dead; `>=TIRAMISU` line ~90 TETAP relevan, JANGAN disentuh), `CleaningReminderWorker.kt` (1 dari 2 site dead — `>=O` dead; `>=TIRAMISU` TETAP relevan), `TrashExpiryWorker.kt` (1 dari 2 site dead — `>=O` dead; `>=TIRAMISU` TETAP relevan).
  - Semua site dead di atas harmless kalau dibiarkan (0 risiko compile/regresi) — TIDAK darurat, lanjut kalau user eksplisit minta lagi.
- **UI STATE ROTASI-SURVIVAL SWEEP (mulai Batch97, ditemukan investigasi mandiri, BUKAN dari `AUDIT_GAP.md`)** — guard project "UI State dan input wajib bertahan dari rotasi (`rememberSaveable`/ViewModel)" belum konsisten diterapkan di seluruh app; `MainActivity.kt`'s `isUnlocked` (App Lock, sebelum Batch97) adalah satu-satunya tempat yang sudah benar. Grep project-wide (`remember { mutableStateOf`) nemu banyak site lain — dikerjakan bertahap per batch (1 file/batch, pola sama kayak P1 #6/P2 #11/P2 #12/Filmstrip dimming), BUKAN 1 batch raksasa (beberapa butuh custom `Saver`, di luar cap 1-file-aman). Status:
  - ✅ **Stage 1 (Batch97)** — `HomeScreen.kt`'s `isSearchActive`/`searchQuery` (search bar Home). SELESAI.
  - ✅ **Stage 2 (Batch98)** — `HomeScreenFolderRow.kt`'s `showRenameDialog`+`text` (rename-folder dialog). SELESAI.
  - ✅ **Stage 3 (Batch99)** — `SwipeScreen.kt`'s `showFullscreen`/`showInfo`/`showSortMenu` (fullscreen viewer, info dialog, sort menu). SELESAI.
  - ✅ **Stage 4 (Batch103)** — `HomeScreenSections.kt`'s `showGoalDialog`+`sliderBytes` (dialog target pembersihan, Boolean+Float). SELESAI.
  - ✅ **Stage 5 (Batch104)** — `TrashScreen.kt`'s `showEmptyTrashConfirm` (dialog konfirmasi kosongkan trash). SELESAI. **Seluruh kandidat AMAN tuntas.**
  - ✅ **Stage 6 (Batch115, kelas custom-`Saver`)** — `TrashScreen.kt`'s `selected` (`SnapshotStateList<Long>`, `listSaver` custom). **CI gagal `run236` (`Unresolved reference: SnapshotStateList`), di-hotfix Batch116** (1 baris import). **[Batch118] Bug SIBLING ditemukan+ditutup** — `listSaver`-nya sendiri TERNYATA sudah benar sejak Batch116, TAPI efek cleanup terpisah di file yang sama (`LaunchedEffect(items.map{it.id}){selected.retainAll(...)}`, ada dari sebelum Stage 6, bukan bagian Batch115/116) diam-diam nge-wipe hasil restore-nya tiap rotasi — baru ketauan sekarang karena baru SEKARANG (pasca Batch117) `TrashScreen` beneran ke-reach saat rotasi. Detail lengkap: entry Batch118 di "Versi Saat Ini". Status Stage 6 sekarang: **selection restore end-to-end sudah bener** (saver + guard cleanup-nya), masih **belum tervalidasi compiler/CI/device asli**.
  - **Sisa kelas custom-`Saver` (3 kandidat, belum disentuh)**: `SwipeScreen.kt`'s `organizeTarget` (`List<MediaItem>?` — trigger dialog Organize di `SwipeScreenControls.kt`'s `customFolder`/`selectedExisting` jadi ikut moot kalau ini sendiri gak survive, karena parent-nya duluan nutup dialog; `MediaItem` belum dikonfirmasi Parcelable-compatible, butuh custom `Saver`/mapping by-id — field-nya sendiri, dicek Batch115, semua primitif/`String`/`Uri`/enum, jadi `mapSaver` layak tanpa `@Parcelize`); `SwipeScreenGrid.kt`'s `zoomedItem` (`MediaItem?`, sama kelas masalah); `SettingsScreen.kt`'s `updateState` (sealed class `UpdateUiState` — custom `Saver` per-subtype, tapi ini arguably OK ilang saat rotasi karena bisa di-re-check ulang, prioritas rendah). Nunggu instruksi eksplisit user buat lanjut, satu per satu.
- **Compose stability gap di `GridSelectContent` (ditemukan Batch90)** — `SwipeScreenGrid.kt`'s `GridSelectContent(items: List<MediaItem>, ..., selected: List<Long>, ...)` pakai tipe `List<T>` polos yang Compose compiler anggap unstable (bukan `ImmutableList`) — fungsi ini kemungkinan gak bisa di-skip compiler sekalipun nilai parameternya gak berubah. Beda level dari fix `derivedStateOf` per-cell yang sudah dikerjakan Batch90 (itu perbaikan SKOPING invalidasi antar-cell; ini perbaikan SKIPPABILITY fungsi itu sendiri, layer terpisah). Perbaikan penuh butuh migrasi ke `ImmutableList`/`@Immutable` wrapper (`kotlinx.collections.immutable` — LIBRARY baru + ubah signature banyak call site) — perubahan arsitektur yang lebih besar dari scope "optimalisasi scrolling" murni yang diminta Batch90, sengaja ditunda sampai user minta eksplisit.
- **✅ NeumorphShape.Button/Chip wiring — SELESAI (Batch91)**. `GlassButton.kt` NEUMORPH branch + `SwipeScreenControls.kt` InfoChip NEUMORPH branch sekarang baca `NeumorphShape.Button`/`.Chip` alih-alih literal `RoundedCornerShape` lama. Semua 3 role (`Card`/`Button`/`Chip`) sekarang wired — inkonsistensi visual (card cut-corner vs button/chip masih rounded) yang naik prioritas sejak Batch85 sudah tertutup. Detail lengkap: Batch91 di `ARCHIVE_HISTORY.md`.
- **`GalleryTypography` (Type.kt) sama gap-nya kayak `NeumorphTypography` sebelum Batch84** (ditemukan Batch84) — grep project-wide nemu `displayLarge`/`headlineSmall`/`labelMedium`/`labelSmall` beneran dipakai (`OnboardingScreen.kt`/`MainActivity.kt`/`HomeScreenSections.kt`/`SettingsScreen.kt`/`TrashScreen.kt`/`HomeScreenFolderRow.kt`) tapi 4 role itu TIDAK didefinisikan di `Type.kt` juga — jadi Signature & Indigo Noir (bukan cuma Amber Reserve sebelum batch ini) sama-sama diam-diam jatuh ke default `Typography()` Compose Material3 yang gak nyambung ke tema manapun di project ini untuk 4 role tsb. Belum diperbaiki batch ini (scope eksplisit user cuma "Neumorphism murni", nyentuh `Type.kt` = ikut ubah Signature/Indigo Noir, di luar permintaan) — nunggu instruksi eksplisit user kalau mau ditutup juga utk 2 theme lain.
- **Light-mode Neumorph panel gap (pre-existing, dicatat lagi Batch85)** — `NeumorphSurface`'s `fillColor`/`pressedFillColor` params masih hardcode ke token dark-mode aja (`Neumorph.NavyCard` dkk), belum baca `*OnLight` variant meski token-nya (termasuk `BorderFadeOnLight`, `ShadowDarkOnLight`/`ShadowLightOnLight` yang baru di-cyan-in Batch85) udah lengkap disiapkan sejak Batch81. Neumorph di light-mode sistem masih belum konsisten sampai gap ini ditutup.
- **REBRANDING Gallery Cleaner → Snaply — ✅ SELESAI (Batch55-57, kosmetik only)**. Diganti (7 file, semua string display): `strings.xml`, `MainActivity.kt`, `CleaningReminderWorker.kt`, `HomeScreen.kt`, `README.md`/`ROADMAP.md`/`RELEASE_SIGNING.md`, `CHANGELOG.md`.
  - **Tetap `GalleryCleaner`/`com.example.gallerycleaner` PERMANEN, sengaja** (nemu ini di kode = by design, bukan bug — lihat juga "IDENTITAS PROJECT" di atas): `applicationId`/package Kotlin (ganti = semua file `.kt` kesentuh, 0 manfaat user-facing); folder Termux/repo GitHub (`-iname` script cari nama ini persis, PascalCase tanpa hyphen — insiden Batch46 kalau salah); `rootProject.name` (internal Gradle); nama class/style resource internal (`GalleryCleanerApp`/`GalleryCleanerTheme`/`Theme.GalleryCleaner`); folder storage on-device (`Pictures|Movies/GalleryCleaner/Backup`, `Documents/GalleryCleaner/logs/` — ganti = backup/log lama user existing nyangkut di folder lama, STABILITY WINS); `AUDIT_GAP.md` (verbatim upload user); Riwayat Batch lama (fakta historis, gak ditulis ulang); nama kompetitor "Sponge - Gallery Cleaner" di `ROADMAP.md`; User-Agent HTTP `"GalleryCleaner-App"`.
  - Rebrand PENUH (package/repo/folder ikut ganti) = keputusan terpisah, butuh izin eksplisit user + langkah manual Termux tambahan (di luar script immutable project ini).

- **AUDIT GAP TRACKER (mulai Batch38)** — 20 temuan P0/P1/P2. Sumber sekarang **`AUDIT_GAP.md` di root repo** (ditanamkan permanen Batch45 — sebelumnya cuma ada sebagai upload chat sesi lama, itu sebabnya sempat jadi BLOCKER di awal sesi ini). Dikerjakan bertahap per batch (bukan 1 batch raksasa — di luar batas 3-file/batch project ini). Status:
  - ✅ **P0 #3** (retention gak auto-eksekusi) — **Batch38**: `TrashExpiryWorker` baru, notifikasi saat item lewat retensi. **Catatan penting**: silent background delete TERBUKTI mustahil di scoped storage (`TrashStore`'s doc sendiri sudah bilang ini sejak sebelum Batch38) — `MediaStore.createDeleteRequest()` WAJIB dari foreground Activity + WAJIB dialog konfirmasi user di API30+. Jadi fix-nya notifikasi proaktif, bukan silent-delete (yang memang gak mungkin). Audit-nya benar soal gejala, tapi solusi "truly automatic" di deskripsi audit gak feasible di platform ini — didokumentasikan biar gak diulang gagal-paham di batch depan.
  - ✅ **P0 #4** (App Lock klaim vs implementasi) — **Batch39**: implementasi lama pakai `KeyguardManager.createConfirmDeviceCredentialIntent()` (deprecated, `@Suppress("DEPRECATION")`) yang cuma nunjukin layar PIN/pattern/password device — "biometric" di README/Settings selama ini cuma klaim, bukan fitur nyata. Fix: ganti ke `androidx.biometric.BiometricPrompt` dengan `BIOMETRIC_STRONG or DEVICE_CREDENTIAL` — sekarang beneran nunjukin prompt sidik jari/wajah dulu, fallback otomatis ke screen lock kalau biometric gak ke-enroll/gagal. Ini fix nyata (bukan cuma benerin dokumentasi), karena implementasi lama emang gak sesuai klaim. Detail lengkap: Batch39 di `ARCHIVE_HISTORY.md`.
  - ✅ **P0 #1** (video belum ke-scan) — **Batch40**: `MediaDataSource` sekarang query `MediaStore.Video.Media` selain `Images.Media`, `MediaItem` punya field `mediaType`/`durationMillis`, manifest+runtime permission `READ_MEDIA_VIDEO` ditambahkan, thumbnail video jalan lewat Coil `VideoFrameDecoder` (didaftarkan sekali di `GalleryCleanerApp`, otomatis berlaku di semua layar via `MediaPreview.kt`), play-badge ditambahkan di `MediaPreview.kt` sebagai satu-satunya penanda visual video. Detail lengkap + apa yang SENGAJA belum dikerjakan (duration label, tap-to-play, filter by type): Batch40 di `ARCHIVE_HISTORY.md`. **[Batch121]** Tap-to-play (1 dari 3 item yang sengaja ditunda) sekarang SELESAI — dipicu laporan bug user (video ikut tampil tapi 0 bisa diputar/diinspeksi), `FullscreenViewer`/`SwipeScreenCard.kt` dapat pemutaran video sungguhan via Media3 ExoPlayer. Duration label & filter by-type MASIH belum dikerjakan. Detail lengkap: entry Batch121 di "Versi Saat Ini" atas.
  - ✅ **P0 #2** ("Trash" bukan trash filesystem) — **Batch42**: investigasi dulu sebelum coding (konsisten pola P0 #3/#4). Cek 3 tempat yang mungkin overclaim: (1) `TrashScreen.kt` copy — udah akurat ("Trash (N)", "Empty Trash", "Delete permanently", warning eksplisit sebelum aksi final, gak pernah klaim file dipindah ke OS trash), (2) `SettingsScreen.kt` retention copy — udah akurat ("flag items in Trash for cleanup after:", bukan "auto-delete"), (3) `README.md` — 0 hit kata "trash" sama sekali, gak ada klaim yang perlu dikoreksi. Kesimpulan: gap-nya murni di level dokumentasi INTERNAL/arsitektur (`TrashStore.kt`'s doc comment), bukan di UX yang dilihat user — pattern "review queue + countdown + restore" yang dipakai app ini sama persis kayak "Recently Deleted" di app galeri lain, jadi bukan bug semantik. Fix: perkuat doc comment `TrashStore.kt` biar eksplisit sebut ini bukan `MediaStore.createTrashRequest()` (API30+, real OS trash), dan jelasin kenapa itu SENGAJA belum diadopsi (butuh re-query trashed items dari MediaStore via `MATCH_TRASHED`, flow untrash terpisah, dan penanganan item yang di-trash app lain muncul di queue — perubahan arsitektur, bukan fix kecil). Didokumentasikan sebagai kandidat batch terpisah kalau OS-level trash jadi requirement produk nyata, BUKAN dikerjakan sekarang biar gak "greedy".
  - ✅ **P1 #5** (progressive loading repeated List copy) — **Batch44**: `MainActivity`'s `allMedia = allMedia + page` bikin FULL copy list yang udah ke-load tiap 1 page baru masuk — O(n) per page, O(n²) total buat seluruh loading di library besar (ribuan-puluhan ribu item, per kata audit). Fix: `allMedia` tipe-nya jadi `PersistentList<MediaItem>` (kotlinx.collections.immutable) — `.addAll(page)` pakai structural sharing, bukan full-copy, jadi cuma O(page.size) per page bukan O(total size so far). Compose reactivity (LaunchedEffect keying, recomposition) TIDAK perlu diubah sama sekali — PersistentList tetep `List`, dan Compose bandingin value pakai `equals()` structural by default, jadi behavior lama 100% preserved, cuma cara nyimpen datanya yang lebih efisien. Detail lengkap (termasuk kenapa pin ke versi 0.3.8, bukan versi terbaru): Batch44 di `ARCHIVE_HISTORY.md`.
  - ✅ **P1 #6** (duplicate scan mahal untuk library besar) — **SELESAI, stage 2b Batch52** (final stage). Riwayat: stage 1 (Batch45, persistent hash cache) → stage 2a (Batch47, `yield()` tiap 20 item + `onProgress` callback backend) → **stage 2b (Batch52, 3 file)**: `MainActivity.kt` — duplicate-scan dicabut dari auto-scan `LaunchedEffect(activeMedia)` (dulu nyatu ke Quick Clean "Duplicate files" tiap kali `activeMedia` berubah), diganti manual (`duplicateScanState`/`duplicateScanProgress`/`duplicateScanJob` + `scanDuplicates()`/`cancelDuplicateScan()`, `Job` disimpan biar bisa `.cancel()`). `HomeScreen.kt` — 4 param baru (`duplicateScanState`, `duplicateScanProgress`, `onScanDuplicates`, `onCancelDuplicateScan`), row baru "Duplicate files" di section SMART DETECTION (sejajar Blurry/Similar photos). `HomeScreenSections.kt` — composable baru `CancellableScanTriggerRow` (progress% + tombol Cancel saat scanning), SENGAJA bukan nambah param opsional ke `ScanTriggerRow` yang udah ada (biar 2 call-site lama blur/near-dup 0 disentuh, sesuai Zero-Unnecessary-Refactor). `ScanState<T>` sepanjang `MediaModels.kt` TIDAK diubah (progress disimpan state terpisah `Float`, bukan payload di `ScanState.Scanning`) — jaga di 3 file, hindari resiko ke blur/near-dup yang pakai class sama. Verifikasi: brace/paren balanced 3 file, `quickCategories`/`smartGroups` (dipakai section lain) gak kesenggol saat cabut blok duplicate dari LaunchedEffect, import `kotlinx.coroutines.Job` ditambah di `MainActivity.kt`.
  - ✅ **P1 #7** (near-duplicate cuma heuristik aHash, potensi false positive/negative) — **Batch54**: audit-nya minta hasil "selalu diposisikan sebagai suggestion, bukan confirmed duplicate" — bukan minta algoritma baru (aHash+Hamming-5 tetap, itu domain terpisah/lebih besar dari 1 batch 3-file). Fix: `SwipeScreen.kt` — banner `GlassCard` (komponen shared yang sama dipakai `SwipeScreenGrid.kt`/`SwipeScreenControls.kt`, jadi konsisten iOS-look/glassmorphism, 0 komponen baru) muncul di atas konten review (berlaku utk Swipe MAUPUN Grid view mode, ditaruh sebelum percabangan viewMode) HANYA saat `group.key == "Similar photos"` — string literal yang sama persis dipakai `HomeScreen.kt` bikin grup ini (pola yang sama juga dipakai "Blurry photos"/"Duplicate files", jadi bukan pola baru). Sengaja key di `group.key`, bukan `displayName` (yang bisa ketiban custom folder label dari `folderLabels`) — biar notice ini gak pernah bisa ke-suppress. Teks: "Suggested matches, not confirmed duplicates — grouped by visual similarity. Review each photo before deleting." "Duplicate files" (`findExactDuplicates`, byte-for-byte hash asli) SENGAJA tidak dapat banner ini — itu memang confirmed, beda kelas dari near-dup. **Catatan efek samping**: nemu `.dp` (androidx.compose.ui.unit.dp) belum pernah dipakai di `SwipeScreen.kt` sebelum batch ini (semua dimension di file itu selama ini datang dari composable lain/tanpa literal dp langsung) — jadi importnya belum ada sama sekali; ditambahkan karena kode baru butuh, bukan refactor tak perlu. Verifikasi: brace/paren balanced (114/114, 193/193), GlassCard/Icon/Row/Spacer semua sudah ke-cover wildcard import yang ada (`androidx.compose.foundation.layout.*`, `androidx.compose.material3.*`) kecuali `Icons.Filled.Info` (baru) dan `GlassCard`+`dp` (baru, sudah ditambah). 0 file lain disentuh — P1 #8/9/10 (verification post-move, batch permission) beda domain sama sekali, gak numpang di batch ini.
  - ✅ **P1 #8** (blur detector threshold hardcoded) — **Batch61**: audit-nya sendiri nyimpulin sama persis kayak P1 #7 kemarin — "hasilnya hanya suggestion, bukan fatal, tapi belum cukup robust" — bukan minta kalibrasi baru per resolusi/low-light/soft-focus (itu tuning pixel-math `laplacianVariance()` tanpa compiler/device buat validasi hasil nyata, resiko tinggi 0 manfaat terukur). Fix: `SwipeScreen.kt` — banner `GlassCard` yang sudah ada dari P1 #7 (Batch54) diperluas: sekarang `heuristicSuggestionText` (val baru, `when` di `group.key`) nge-cover baik `"Similar photos"` MAUPUN `"Blurry photos"`, teks beda per grup ("Suggested as possibly blurry — an automatic estimate, not a guarantee. Intentional soft-focus shots can be flagged too."). 0 komponen baru, 0 import baru (`Icon`/`GlassCard`/`dp` udah ada dari Batch54), berlaku Swipe+Grid view mode (posisi banner gak dipindah, masih sebelum percabangan `viewMode`). `laplacianVariance()`/`BLUR_VARIANCE_THRESHOLD` di `MediaScanner.kt` 0 disentuh — catatan kalibrasi (resolusi/low-light/soft-focus) dari audit TETAP jadi limitasi terdokumentasi, bukan diklaim selesai. Verifikasi: brace 115/115, paren 197/197.
  - ✅ **P1 #9** (move/organize partial-success edge case) — **Batch62**: beda kategori dari P1 #7/#8 (yang murni UX-reframing) — audit ini nunjuk bug nyata: `MoveHelper.moveViaDirectFile` (API 24-28 legacy path) fisik-mindahin file dulu baru update row MediaStore terpisah; kalau `ContentResolver.update()` gagal (throw ATAU diam-diam matched 0 rows — yang kedua ini bahkan gak dicek sama sekali sebelumnya), kode lama LANGSUNG lapor `Result.Success` tanpa verifikasi apa pun ("asumsikan media scanner nanti nyusul sendiri") — `applyOrganizeResult` di `MainActivity.kt` percaya penuh, update `relativePath` lokal, padahal MediaStore aslinya belum tentu sinkron; query fresh berikutnya (mis. restart app) bisa nunjukin folder lama lagi. Fix: `MoveHelper.kt` — `moveTo`/`moveViaDirectFile` jadi `suspend`, 2 branch ambigu (rows=0 dan catch-Exception) sekarang `awaitScanConfirms()` (bungkus `MediaScannerConnection.scanFile`'s callback via `suspendCancellableCoroutine`) — beneran nunggu konfirmasi scanner, bukan fire-and-forget — baru putuskan `Result.Success` (kalau scanner konfirmasi) atau `Result.PartialSuccess` (kalau tetap gak kekonfirmasi) — sealed class baru. Happy-path (rows>0, no exception) 0 berubah, tetap fire-and-forget synchronous, 0 latency tambahan buat kasus normal. `moveViaRelativePath` (API 29+) 0 disentuh — one-shot atomic update, structurally gak punya celah partial-success yang sama (dikonfirmasi lewat baca ulang implementasinya, bukan asumsi). `MainActivity.kt` — 2 titik pemanggil `MoveHelper.moveTo` (retry-path + direct-loop) di-update: `PartialSuccess` DIPISAH dari `movedIds` (sengaja gak ikut optimistic local-state update — itu justru sumber bug-nya), dapet snackbar terpisah ("belum terverifikasi — buka ulang app untuk sinkronkan"), dan dikecualikan dari retry-list permission-dialog (retry file yang udah pindah lokasi cuma bakal gagal, sourceFile-nya udah gak ada di path lama). Verifikasi: `MoveHelper.kt` brace 27/27 paren 94/94; `MainActivity.kt` brace 281/281 paren 515/515. Belum tervalidasi compiler/device asli.
  - ✅ **P1 #10** (batch permission belum optimal) — **Batch64**: investigasi dulu (pola sama kayak P0 #3/#4/P1 #8) — ternyata `performOrganize` di `MainActivity.kt` UDAH punya jalur batch-optimal: di API30+ (`MoveHelper.supportsBatchWriteRequest()`), seluruh selection dapet SATU dialog consent via `MediaStore.createWriteRequest()` (persis pola yang sama dipakai `ImageCompressor` buat kompresi massal) — bukan gap. Gap SEBENARNYA cuma di jalur legacy API24-29 (`else` branch): `MoveHelper.moveTo` dipanggil satu-satu dalam loop, `break` begitu ketemu `NeedsPermission` pertama, lalu retry ulang SEMUA item sisa setelah grant — tapi `RecoverableSecurityException`'s `IntentSender` di API<30 cuma nge-grant izin buat SATU file yang motret exception itu (bukan batch), jadi kalau ada beberapa item lain yang JUGA butuh grant terpisah, user bisa kena serangkaian dialog sistem satu-per-satu berturut-turut — dan **tidak ada API OS buat batch-grant di bawah API 30 sama sekali** (`MediaStore.createWriteRequest()` sendiri baru ada dari API30), jadi ini limitasi platform, bukan bug kode yang bisa "dibenerin" beneran (sama persis pola P0 #3/#4: audit benar soal gejala, tapi solusi "truly batched" gak feasible di OS lama). Fix: `SwipeScreenControls.kt`'s `OrganizeFolderDialog` — note kecil ("Your Android version may show a separate permission prompt for each photo that needs one.") muncul HANYA saat `itemCount > 1 && !MoveHelper.supportsBatchWriteRequest()` — set ekspektasi user di depan (sebelum dialog2 bertubi-tubi muncul dan kelihatan kayak bug), bukan klaim "sekarang lebih cepat". Single-item move (swipe biasa) 0 kena efek — cuma trigger buat multi-select "Organize N" dari grid mode. 0 komponen baru, `MoveHelper` udah visible tanpa import baru (1 package sama `com.example.gallerycleaner`). Verifikasi: brace/paren balanced (41/41, 140/140). **⚠️ Temuan sampingan (di luar scope batch ini, cuma dicatat)**: instruksi sistem project ini bilang minSdk WAJIB 31, tapi `app/build.gradle.kts` aktual `minSdk = 24` — dan sengaja, karena arsitektur `MoveHelper`/`ImageCompressor` punya banyak percabangan API24/29/30 yang cuma masuk akal kalau minSdk beneran 24 (kalau minSdk 31, seluruh jalur legacy P1 #10 ini — termasuk fix batch ini — jadi dead code). TIDAK diubah (ganti minSdk = task arsitektur besar terpisah, butuh izin eksplisit user), cuma di-flag di sini biar user sadar ada gap instruksi-vs-kode ini. **[KOREKSI Batch105]**: gap ditutup — `minSdk` dinaikkan ke 31 atas instruksi eksplisit user (detail: entry Batch105 di "Versi Saat Ini"). Jalur legacy P1 #10 di atas sekarang structurally dead code, sengaja belum dihapus (cleanup = task terpisah).

  - ✅ **AUDIT GAP TRACKER P0+P1 SELESAI SEMUA (#1-10)** — sisa cuma P2 #11-20 (polish/production quality, prioritas lebih rendah per `AUDIT_GAP.md`).
  - ✅ **P2 #11** (hardcoded UI strings, `strings.xml` sudah ada tapi 0 dipakai) — **SELESAI SEMUA 4 STAGE, final Batch71**. **Stage 1 (Batch67)**/**Stage 2 (Batch69)**/**Stage 3 (Batch70)**: lihat riwayat batch untuk detail `OnboardingScreen.kt`/`SettingsScreen.kt`/`TrashScreen.kt`. **Stage 4 (Batch71, 2 file, FINAL)**: `HomeScreen.kt` — 24 entry baru prefix `home_*` (placeholder search, title app-name — reuse `R.string.app_name` yang udah ada bukan bikin duplikat "Snaply" baru, 6 content-description termasuk 2 varian random-mode, label Trash + varian count, empty-state, 4 section label, title+subtitle 3 scan row termasuk 2 format string count dan 1 format string 3-arg buat near-duplicate yang punya pluralisasi "group"/"groups"). **Keputusan penting (beda dari stage 1-3): 6 literal `MediaGroup("...", ...)` key SENGAJA TIDAK diubah** — `"Largest files"`, `"On this day"`, `"Blurry photos"`, `"Similar photos"`, `"Duplicate files"` (+ 1 lagi `"Search results"` di `SearchResultsContent` call site) bukan display text murni: dipakai sebagai (1) key persistensi `progressStore.progressFlow(group.key)`/`saveProgress(group.key, ...)` di `SwipeScreen.kt`/`HomeScreenFolderRow.kt`, dan (2) untuk 2 di antaranya ("Blurry photos"/"Similar photos") juga perbandingan literal `when (group.key)` di `SwipeScreen.kt` (banner suggestion, lihat P1 #7/#8 Batch54/61) — persis pola yang sama sudah didokumentasikan Batch54 ("Sengaja key di `group.key`, bukan `displayName`... biar notice ini gak pernah bisa ke-suppress"). Kalau nanti di-`stringResource()`-kan dan project mulai multi-bahasa (ROADMAP Fase D, belum mulai), key ini bisa ikut berubah per-locale dan MERUSAK progress tersimpan + perbandingan cross-file — jadi tetap literal String Kotlin murni, bukan gap yang perlu diperbaiki. `ScanTriggerRow`'s `title=` param (instance literal terpisah, kebetulan teksnya sama) AMAN diekstrak karena cuma dirender, 0 dipakai buat perbandingan/key di mana pun — dikonfirmasi via grep menyeluruh sebelum eksekusi. Verifikasi: brace 92/92, paren 201/201, grep manual 0 sisa `Text("`/`contentDescription = "`/`title = "`/`subtitle = "` literal (kecuali 6 `MediaGroup` key yang memang sengaja), `strings.xml` well-formed (116 entry total project, 0 duplikat nama).
  - **AUDIT GAP TRACKER P0+P1+P2#11+P2#12 SELESAI SEMUA** — sisa P2 #13-20 (polish/production quality lain, belum digarap).
  - ✅ **P2 #15** ("Versioning CI tidak konsisten" — `versionName = 1.0.<git commit count>` vs release tag `v1.0.<github.run_number>`) — **Batch83**: dikonfirmasi via inspeksi APK terpasang user (versionCode 84 hasil `git rev-list --count`, sementara nama file APK-nya sendiri bilang 205 = `GITHUB_RUN_NUMBER` saat itu). **Koreksi catatan lama** (baris "Belum Dikerjakan" di bawah, dulu ditandai "SUDAH FIXED" — itu SEBAGIAN benar: Batch43 cuma nyamain APK filename + GitHub Release tag supaya keduanya pakai `$GITHUB_RUN_NUMBER`, TAPI versionCode/versionName Gradle asli di `app/build.gradle.kts` — yang dibaca PackageManager/BuildConfig, bukan sekadar nama file — tetap `git rev-list --count HEAD` sampai batch ini. Root cause sebenarnya BELUM pernah disentuh, cuma gejala luarnya). Fix nyata sekarang: `app/build.gradle.kts` — `gitCommitCount()` diganti `ciVersionCode()` (baca `GITHUB_RUN_NUMBER`, fallback 1 untuk build lokal). `build.yml` — step baru "Verify versionCode matches GITHUB_RUN_NUMBER" disisipkan sebelum "Rename APK", pakai `aapt dump badging` buat baca versionCode/versionName ASLI dari APK hasil build dan `exit 1` kalau beda dari `$GITHUB_RUN_NUMBER` — supaya drift jenis ini gak bisa lolos diam-diam lagi. `UpdateChecker.kt` sengaja **TIDAK disentuh** — class itu bandingin tag string GitHub Release (bukan versionCode numerik), jadi 0 kesenggol oleh fix ini; doc comment class itu ("skema git-rev-count vs run_number beda") sekarang secara teknis SUDAH TIDAK BERLAKU LAGI tapi dibiarkan (historis, harmless, di luar scope 3-file batch ini — bukan bug, cuma komentar basi). Sisa `AUDIT_GAP.md`: P2 #13-14, #16-20.
  - ✅ **P2 #13** (README divergen dari implementasi — klaim photos/videos, PIN/biometric, retention automatic) — **verifikasi Batch92, 0 file kode berubah**. Audit ini basis-nya `v37` (sebelum P0 #1/#4 dikerjakan) — dibaca ulang `README.md` baris-per-baris terhadap kode SEKARANG: (1) "Loads all photos & videos from the device via `MediaStore`" — SEKARANG akurat, video sudah di-scan sejak P0 #1/Batch40; (2) "App lock: biometric prompt (fingerprint/face) with automatic fallback to your device's PIN/pattern/password" — SEKARANG akurat, `BiometricPrompt` asli sejak P0 #4/Batch39 (bukan lagi cuma `KeyguardManager` device-credential); (3) retention/"automatic cleanup" — grep `README.md` 0 hit kata "trash"/"automatic" terkait retention (konsisten temuan investigasi P0 #2/Batch42), jadi 0 klaim yang perlu dikoreksi di sini juga. Ke-3 divergensi yang di-flag audit sudah tertutup sebagai efek samping batch P0 lama — **bukan pekerjaan baru, cuma verifikasi+tandai selesai di tracker** biar sesi depan gak re-investigasi gap yang sama.
  - ✅ **P2 #18** (exact duplicate belum punya persistent hash cache) — **sudah resolved sejak Batch45 sebagai bagian P1 #6 stage 1, dikonfirmasi Batch92**. Temuan #18 di `AUDIT_GAP.md` dan P1 #6 sama-sama mendeskripsikan gap yang SAMA PERSIS (re-hash file yang sama tiap scan) — dari dokumen audit asli yang menomori 2 kali di section berbeda (P1 #6 vs P2 #18), bukan 2 gap independen. Dikonfirmasi via kode: `MediaScanner.kt`'s `findExactDuplicates()` line ~115 sudah instantiate `HashCacheStore(context)` (kelas dedicated dibuat Batch45, doc comment-nya sendiri eksplisit bilang "Added Batch45 (Audit Gap P1 #6, stage 1)"), cache hit divalidasi terhadap `sizeBytes`+`dateModifiedMillis` biar aman dari file yang diedit in-place. 0 kerja baru dibutuhkan — cuma nutup entry tracker yang kelewat ke-mark selesai waktu P1 #6 ditutup.
  - **Sisa `AUDIT_GAP.md` yang BENERAN belum digarap (dikoreksi Batch92, sebelumnya salah nyebut #13/#18 ikut nunggu)**: **P2 #14** (Gradle Wrapper — **blocked**, butuh download `gradle-wrapper.jar` binary dari internet, sandbox ini 0 akses network, lihat `<network_configuration>`; menulis `gradlew`/`gradlew.bat`/`gradle-wrapper.properties` TANPA jar-nya cuma wrapper yang gak bisa jalan, ZERO-HALLUCINATION melarang bikin artefak yang keliatan lengkap tapi sebenarnya rusak), **P2 #16** (`isMinifyEnabled = false` di `app/build.gradle.kts` release — audit sendiri bilang "layak dipertimbangkan setelah rules diuji") — **[Batch111] DIEKSEKUSI**: `isMinifyEnabled`/`isShrinkResources` sekarang `true`, dibarengi 4 keep-rule extra-protection baru di `proguard-rules.pro` (detail: entry Batch111 "Versi Saat Ini" + bullet app-size "Belum Dikerjakan"). Status **pending validasi** (compiler/CI/device asli, sandbox 0 akses ke situ) — BUKAN "selesai total" sampai build CI hijau + user konfirmasi fitur App Lock/Reminder/Trash-expiry/Update-checker/CrashLogger tetap jalan normal di APK asli., **P2 #17** (`android:largeHeap="true"` di `AndroidManifest.xml`, protected file — audit-nya sendiri bilang ini "red flag" tapi solusinya "jangan bergantung pada large heap sebagai solusi utama", yaitu perubahan arsitektur memory management, BUKAN sekadar hapus 1 baris manifest yang bisa langsung OOM tanpa profiling nyata — nunggu instruksi eksplisit user), **P2 #19-20** (instrumentation test coverage + verifikasi lintas-versi Android — butuh device/emulator/compiler asli, 0 tersedia di sandbox ini, sama limitasi yang sudah dicatat berkali-kali di "Belum Dikerjakan" bawah).
  - ✅ **Restyling Indigo Noir → "Cupertino Style" murni** (permintaan baru user, di luar `AUDIT_GAP.md`) — **SELESAI SEMUA 2 STAGE, final Batch77**. Stage 1 (Batch74): token+surface foundation, 0 file existing disentuh. Stage 2 (Batch77, FINAL): wiring `MaterialStyle.kt`+`GlassCard.kt`+`GlassButton.kt`+`SwipeScreenControls.kt` (4 file sekaligus, > cap 3-file — justifikasi STABILITY WINS vs Kotlin exhaustive-`when`, sudah diflag sejak Batch74). Indigo Noir sekarang render panel/card/button/chip lewat `MaterialStyle.CUPERTINO` murni (bukan lagi reskin `glassPanel()` Signature). Detail lengkap: `PROJECT_STATE.md` Batch77 (bawah) + Batch74.
    **Stage 1 (Batch74, 2 file baru, 0 file existing disentuh — 0 risiko regresi)**: `CupertinoTokens.kt` (baru) — object `Cupertino` standalone, SEMUA value alias langsung dari palette Indigo Noir yang SUDAH ADA di `Color.kt` (`IndigoBg`/`IndigoSurface`/`PlatinumText`/`PeriwinkleKeep`/dst — bukan warna baru, bukan pinjam dari `MidnightGlass`/`SkeuoLite`/`Neumorph`) + 2 primitif turunan baru yang genuinely iOS-specific: `ShadowSoft` (shadow tunggal hitam alpha 20%, BUKAN shadow ganda ala Neumorph, BUKAN shadow bertint warna ala Glass) dan `Hairline` (separator garis tipis dari `PlatinumText` alpha 12%, ganti visible gradient edge ala glass). `PRESSED_ALPHA` (0.72f) buat mekanisme tekan iOS asli (dim-via-opacity, BUKAN ganti warna kayak Glass, BUKAN hilang-shadow kayak Neumorph — mekanisme berbeda, konsisten sama requirement Batch27/36 "beda mekanisme bukan cuma beda warna"). Light-mode counterpart didapat mekanis dari alias yang sudah ada juga (`LilacBg`/`LilacSurfaceRaised`/dst).
    `CupertinoSurface.kt` (baru) — Composable, pola early-return SAMA kayak `NeumorphSurface` (dipanggil dari `GlassCard`), TAPI resep beda total: 1 shadow lembut (bukan 2 offset kayak Neumorph), fill flat opaque + hairline border OPSIONAL 0.5dp (bukan 0 border kayak Neumorph, bukan gradient border kayak Glass), tekan-state via `Modifier.alpha()` di outer Box (dim shadow+fill+content bareng sebagai 1 layer compositing) — parameter `pressed: Boolean` caller-driven, IDENTIK signature style `NeumorphSurface` (shape/fillColor/shadowElevation/contentPadding/onClick/enabled/interactionSource) biar konsisten gampang dipanggil dari `GlassButton` stage 2 nanti.
    **BELUM diaktifkan** — `MaterialStyle` enum BELUM ditambah `CUPERTINO`, `materialStyleFor()` BELUM diubah, jadi Indigo Noir MASIH render Glass seperti biasa sampai stage 2. Alasan dipisah 2 stage: nambah 1 `MaterialStyle` enum entry bikin SEMUA `when(style)` yang exhaustive (`GlassCard.kt`, `GlassButton.kt`, `SwipeScreenControls.kt`'s `InfoChip`) GAGAL COMPILE kalau gak di-update BARENGAN — itu berarti stage 2 WAJIB nyentuh ke-4 file itu SEKALIGUS (bukan pilihan, syarat Kotlin exhaustive-`when`), yang artinya bakal MELEBIHI cap 3-file — sudah diflag di sini biar transparan, bukan pelanggaran diam-diam: **STABILITY WINS** (kode harus tetap compile di setiap batch boundary) menang lawan cap-3-file dalam kasus spesifik ini.
    **Ditemukan sambil lalu (BUKAN scope batch ini, dicatat aja)**: `app/build.gradle.kts` masih `minSdk = 24`, padahal instruksi project eksplisit bilang "WAJIB Native Kotlin + Jetpack Compose murni (minSdk = 31)" — belum pernah dibenerin di batch manapun yang tercatat. `build.gradle.kts` protected file, di luar scope restyle ini, nunggu instruksi eksplisit user (menaikkan minSdk bisa mempersempit device support, keputusan besar sendiri). **[KOREKSI Batch105]**: sudah dinaikkan ke 31 atas instruksi eksplisit user — detail di entry Batch105 ("Versi Saat Ini").
    Verifikasi: brace/paren balanced kedua file baru (1/1+34/34, 6/6+35/35). Semua 12 token `Color.kt` yang di-alias dikonfirmasi ADA (grep). Belum tervalidasi compiler/device asli.
  - ✅ **P2 #12** (pluralization manual, pola `photo${if (x==1) "" else "s"}`) — **SELESAI SEMUA 2 STAGE, final Batch73**. **Stage 1 (Batch72)**: lihat riwayat batch untuk detail `MainActivity.kt` (4 `<plurals>`)/`HomeScreenSections.kt` (1 `<plurals>`). **Stage 2 (Batch73, 2 file, FINAL)**: `HomeScreen.kt`'s near-dup subtitle ("X photos in Y group(s) — tap to review") — dikonversi dari format-string 3-arg (`home_near_dup_found`, workaround sementara dari Batch71 yang masih nyimpen logika `if (s.result.size != 1) "s" else ""` di Kotlin) jadi `<plurals name="home_near_dup_found_groups">` proper (quantity bucket `one`/`other` nentuin "group" vs "groups", TANPA logika if manual sama sekali di Kotlin lagi). Resource lama `<string name="home_near_dup_found">` DIHAPUS (superseded, bukan APK-breaking karena baru dipakai internal Batch71-72, belum pernah dirilis). Nama `<plurals>` sengaja BEDA dari nama string lama (`home_near_dup_found_groups`, bukan reuse nama `home_near_dup_found`) buat hindari resource-name collision `R.string.*` vs `R.plurals.*` — dicek eksplisit 0 collision. Kata "photos" (total foto) TETAP hardcoded plural literal di kedua quantity item (BUKAN scope perbaikan — audit P2 #12 spesifik nargetin pola `if`-manual, dan "photos" di situ dari awal emang selalu plural literal, bukan hasil `if`, jadi bukan gap yang di-flag; kalau mau dibenerin juga perlu `<plurals>` bersarang dua level yang gak didukung format Android biasa, di luar scope). `pluralStringResource()` (API Compose, BEDA dari `getQuantityString()` yang dipakai stage 1) dipakai di sini karena titik panggilnya ada LANGSUNG di body @Composable (`ScanTriggerRow`'s `subtitle =` param, evaluasi saat composition) — BUKAN di dalam `remember{}`/callback kayak titik-titik stage 1, jadi API Compose-nya valid dipanggil langsung, konsisten sama pola `stringResource()` yang udah dipakai di file yang sama.
    Dengan ini **P2 #12 (pluralization, seluruh 2 stage) SELESAI TOTAL**. Sisa `AUDIT_GAP.md`: P2 #13-20 (README divergence, Gradle Wrapper, versioning CI, dst — belum digarap).
    Verifikasi: brace 92/92 paren 200/200 (`HomeScreen.kt`). Grep manual project-wide (`MainActivity.kt`+`HomeScreenSections.kt`+`HomeScreen.kt`+seluruh file lain): 0 sisa pola `!= 1) "s"`/`== 1) ""` di manapun. `strings.xml`: 115 `<string>` (1 dihapus dari 116) + 6 `<plurals>` total, 0 duplikat nama, 0 collision cross-type. Belum tervalidasi compiler/device asli.
  - **Cleanup pending approval (Batch36)** — `SkeuoLiteTokens.kt` + `SkeuoModifier.kt` sekarang 100% unreferenced oleh `MaterialStyle`/tema manapun (`AMBER_RESERVE` pindah ke `NEUMORPH`), tapi TIDAK dihapus batch ini (butuh izin eksplisit user per aturan project). Ikut jadi dead-once-approved: 13 `val` warna lama di `Color.kt` (`EspressoSurface`, `EspressoOutline`, `IvoryText`, `IvoryTextSecondary`, `BrassKeep`, `BrassKeepDim`, `BrassKeepOnLight`, `CreamBg`, `CreamSurfaceRaised`, `CreamOutline`, `EspressoTextPrimary`, `EspressoTextSecondary`, `EspressoBg`) yang cuma dipakai `SkeuoLiteTokens.kt`. Juga ditemukan (bukan dari batch ini, sudah mati dari sebelumnya): `EspressoSurfaceRaised`, `OxbloodDeleteDim` — 0 referensi di manapun. Semua di-flag di sini, tunggu izin user sebelum hapus.
- **`CHANGELOG.md` staleness gap (ditemukan Batch76, diperluas Batch80, diperluas lagi Batch107)** — entri Batch64-75 (12 batch) gak pernah ditambahkan (berhenti di Batch63), padahal `PROJECT_STATE.md` tetap konsisten ter-update tiap batch. Gap sekarang juga mencakup **Batch78-79** (NeumorphSurface stack/kontras) — 2 batch itu belum sempat dicatat ke `CHANGELOG.md` sebelum Batch80 ditulis. **[Batch107]**: ditemukan gap ketiga — `CHANGELOG.md` berhenti lagi di `v96_Batch99`, entri **Batch100-106** (7 batch) gak pernah ditambahkan; `PROJECT_STATE.md` tetap konsisten ter-update sepanjang rentang itu (pola sama seperti 2 gap sebelumnya). `CHANGELOG.md` mulai ditulis lagi dari Batch107 ini dan seterusnya (append-only, sesuai SOP). Ketiga rentang (Batch64-75, 78-79, 100-106) belum di-backfill (sengaja TIDAK ditulis ulang sekarang biar gak nebak-nebak tanggal/run-number yang gak tercatat pasti di mana pun) — nunggu instruksi eksplisit user kalau mau entri-entri lama ditulis ulang ringkas ke `CHANGELOG.md`.
- **ROADMAP Fase C item 9** — Phase-1b package restructure (flat → real sub-package `com.example.gallerycleaner.data.media` dst.) — masih pending, butuh compiler/CI nyata per-layer, tidak tersedia di sandbox ini.
- **ROADMAP Fase C item 8** (keputusan cascade `MidnightSkeuoButton`) — **superseded/tidak relevan lagi**: sistem Midnight Skeuo dihapus total sejak Batch21 (diganti Glassmorphism), lalu Batch27 memperkenalkan `SkeuoLite` terpisah untuk Amber Reserve. Tidak perlu keputusan lanjutan.
- **ROADMAP Fase D (belum dimulai)**: multi-bahasa (minimal ES + PT-BR), monetisasi premium one-time-purchase, Play Store readiness (privacy policy URL, Data Safety form, screenshot set, ASO description).
- **Filmstrip belum secara visual meredupkan item yang sudah di-organize — ✅ SELESAI (Batch93)**, **+ gap analog delete — ✅ SELESAI (Batch94)**. Detail: Batch93/Batch94 di `ARCHIVE_HISTORY.md`.
- Belum ada test end-to-end manual di device asli untuk jalur legacy Organize (API 24-28) — belum ada emulator/compiler di sandbox ini.
- ~~Temuan lama: nama file APK vs nomor tag GitHub Release pakai 2 skema angka berbeda~~ — **SUDAH FIXED (hanya lapisan luar)**: `build.yml`'s APK filename + GitHub Release tag (Batch43) memang sudah konsisten `$GITHUB_RUN_NUMBER`. **KOREKSI PENTING (Batch83)**: baris ini dulu (Batch63) kebaca seolah SELURUH masalah sudah kelar — ternyata belum. `app/build.gradle.kts`'s versionCode/versionName Gradle ASLI (yang ke-bake ke APK, dibaca PackageManager/BuildConfig — beda dari sekadar nama file) tetap pakai `git rev-list --count HEAD` sampai Batch83, gak pernah disentuh Batch43. Root cause sebenarnya baru bener-bener fixed Batch83 — lihat entry "P2 #15" di AUDIT GAP TRACKER atas untuk detail lengkap. Dibiarkan tercoret (bukan dihapus) biar histori investigasi Batch63→Batch83 tetap kebaca sesi depan.
- **Neumorph panel light-mode fill gap (ditemukan Batch81, pre-existing dari Batch36)** — `NeumorphSurface`'s `fillColor`/`pressedFillColor` default hardcode ke `Neumorph.NavyCard`/`Neumorph.DeepNavy` (dark-mode token doang), dan `GlassCard.kt` (satu-satunya jalur panel Neumorph dipanggil dari seluruh app) gak pernah override keduanya. `AmberReserveLight`'s `ColorScheme` (`Theme.kt`) sendiri sudah bener baca `Neumorph.LightBg`/`LightCard`/dst., tapi PANEL-nya (bukan background/text M3) bakal tetap render fill dark-mode kalau app di-switch ke light mode. Tidak diperbaiki Batch81 (di luar scope permintaan user batch itu, butuh keputusan wiring `isSystemInDarkTheme()`/`darkTheme` param terpisah) — nunggu instruksi eksplisit user.

## Protected Assets (jangan hapus/replace penuh)
- app/build.gradle.kts, build.gradle.kts, settings.gradle.kts
- app/src/main/AndroidManifest.xml
- .github/workflows/build.yml
- .gitignore
- release.keystore (tidak disertakan di repo, via secrets)

## ⚠️ Insiden Operasional (permanen)
- **Aturan (asal: Batch46)**: nama folder/repo Termux WAJIB PERSIS `GalleryCleaner` (PascalCase, tanpa hyphen) di tiap `-iname`/fallback path skrip. Precedence: nama repo GitHub existing > gaya penamaan umum project.

## Riwayat Batch
📦 Seluruh histori batch (Batch1-101) diarsipkan total: lihat `ARCHIVE_HISTORY.md` di root repo.

## [RESUME POINT]
- Last action: **Batch136 — fix gesture volume yang cuma kosmetik (0 ngaruh ke master volume device), DIEKSEKUSI.** User bug report + IMPLISIT konfirmasi device-test Batch131-135 lainnya OK ("semua sudah sesuai cara kerja batch sebelumnya"). Root cause + fix lengkap: entry [Batch136] di "Belum Dikerjakan" atas — CHANGELOG.md diupdate (entry Batch136, append di atas Batch135).
- **STACKING WARNING Batch131-135 (5 batch) — RESOLVED sebagian oleh laporan user hari ini**: device-test riil (install+run APK) MEMBUKTIKAN compile+build sukses utk kelimanya, dan user 0 lapor regresi lain selain gesture volume → checklist Batch135 (nav fade-in balik, label Brightness/Volume, drag brightness/volume jalan) dianggap PASS implisit dari kalimat "semua sudah sesuai". **TIDAK 100% device-test eksplisit per-poin** (user 0 kasih konfirmasi checklist item-per-item) — kalau ternyata ada 1 dari situ yang sebenarnya masih gagal tapi belum ketauan user, itu baru akan muncul sebagai laporan baru terpisah, jangan diasumsikan proaktif tanpa laporan. Batch136 (di atas base yang sekarang jauh lebih confirmed ini) TETAP 100% belum tervalidasi build/device asli sendiri — **JANGAN eksekusi apapun lagi di `SwipeScreenCard.kt` sampai user push+build+device-test Batch136 ini**, kecuali user kasih izin eksplisit utk menumpuk lagi (pola precedent Batch133/134/136).
- **Device-test checklist Batch136 (WAJIB, lihat detail lengkap di entry [Batch136] "Belum Dikerjakan")**:
  1. **[paling kritis]** Geser kanan (volume) pas video main → volume MEDIA device beneran naik-turun (cek notification shade/tombol fisik hp), BUKAN cuma bar indikator internal doang.
  2. Ganti video (swipe) sambil viewer masih kebuka → level volume TIDAK ke-reset paksa ke 100%.
  3. Mute button (Batch133) & drag brightness (kiri, Batch134) MASIH jalan normal — 0 disentuh batch ini tapi 1 composable sama, regresi turunan paling beresiko kalau ada.
- Kalau user lapor "regresi"/"rusak" lagi tanpa log/screenshot ke depannya: pola yang kepake Batch132/136 — grep dulu titik yang paling MUNGKIN jadi biang (contoh: `\.volume\s*=` / `manualVolume`), baca ulang commit/batch TERAKHIR yang nyentuh area itu, cek apa ada DEFAULT VALUE yang diubah tanpa instruksi eksplisit user (pola regresi paling gampang kejadian: nambah toggle baru dengan default yang DIAM-DIAM ngubah perilaku lama). Jangan tunggu log kalau diagnosis udah pasti dari source sendiri.
- Belum ada konfirmasi device-test terpisah untuk: 5 poin kontrol video Batch127, ATAU setting Enable share text Batch131 — CI hijau cuma jamin COMPILE, bukan PERILAKU runtime di device asli.
- Fase E sisa 7 sub-item "Cleaning Options" (`ROADMAP.md` #14) + item #16-20 (History/Home dashboard/Customize view/Notifications split/My Account) — masih nunggu giliran, TAPI Batch132 (fix regresi) sengaja motong antrian di depan Fase E krn STABILITY WINS > progress fitur baru.
- Video stuttering (Batch125/126, masih flagged, belum digarap) masih nunggu giliran juga, di luar Fase E.
